#ifndef MUTEX_H_
#define MUTEX_H_

#define Mutex_Init() \
    do {             \
    } while ( 0 )

#define Mutex_lock( x, y ) \
    do {                   \
    } while ( 0 )

#define Mutex_unlock( x, y ) \
    do {                     \
    } while ( 0 )

#define Mutex_destroy( x, y ) \
    do {                      \
    } while ( 0 )

#endif // MUTEX_H_
