#ifndef IOT_MUTEX_H
#define IOT_MUTEX_H

/** ============================[ Macros ]============================ */

#define Mutex_Init() \
    do {             \
    } while ( 0 )

#define Mutex_lock( x, y ) \
    do {                   \
    } while ( 0 )

#define Mutex_unlock( x, y ) \
    do {                     \
    } while ( 0 )

#define Mutex_destroy( x, y ) \
    do {                      \
    } while ( 0 )

#endif // IOT_MUTEX_H
