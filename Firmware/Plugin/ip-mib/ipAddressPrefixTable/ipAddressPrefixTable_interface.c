/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.67 $ of : mfd-interface.m2c,v $ 
 *
 * $Id$
 */
/*
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 * ***                                                               ***
 * ***  NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE  ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THIS FILE DOES NOT CONTAIN ANY USER EDITABLE CODE.      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THE GENERATED CODE IS INTERNAL IMPLEMENTATION, AND      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***    IS SUBJECT TO CHANGE WITHOUT WARNING IN FUTURE RELEASES.   ***
 * ***                                                               ***
 * ***                                                               ***
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 */

/*
 * include our parent header 
 */
#include "ipAddressPrefixTable_interface.h"
#include "Assert.h"
#include "BabySteps.h"
#include "Client.h"
#include "Debug.h"
#include "Logger.h"
#include "Mib.h"
#include "RowMerge.h"
#include "TableContainer.h"
#include "ipAddressPrefixTable_constants.h"
#include "ipAddressPrefixTable_data_access.h"
#include "siglog/agent/mfd.h"

/**********************************************************************
 **********************************************************************
 ***
 *** Table ipAddressPrefixTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * IP-MIB::ipAddressPrefixTable is subid 32 of ip.
 * Its status is Current.
 * OID: .1.3.6.1.2.1.4.32, length: 8
 */
typedef struct ipAddressPrefixTable_interface_ctx_s {

    Container_Container* container;
    Cache* cache;

    ipAddressPrefixTable_registration* user_ctx;

    TableRegistrationInfo tbl_info;

    BabyStepsAccessMethods access_multiplexer;

} ipAddressPrefixTable_interface_ctx;

static ipAddressPrefixTable_interface_ctx ipAddressPrefixTable_if_ctx;

static void
_ipAddressPrefixTable_container_init( ipAddressPrefixTable_interface_ctx*
        if_ctx );
static void
_ipAddressPrefixTable_container_shutdown( ipAddressPrefixTable_interface_ctx* if_ctx );

Container_Container*
ipAddressPrefixTable_container_get( void )
{
    return ipAddressPrefixTable_if_ctx.container;
}

ipAddressPrefixTable_registration*
ipAddressPrefixTable_registration_get( void )
{
    return ipAddressPrefixTable_if_ctx.user_ctx;
}

ipAddressPrefixTable_registration*
ipAddressPrefixTable_registration_set( ipAddressPrefixTable_registration*
        newreg )
{
    ipAddressPrefixTable_registration* old = ipAddressPrefixTable_if_ctx.user_ctx;
    ipAddressPrefixTable_if_ctx.user_ctx = newreg;
    return old;
}

int ipAddressPrefixTable_container_size( void )
{
    return CONTAINER_SIZE( ipAddressPrefixTable_if_ctx.container );
}

/*
 * mfd multiplexer modes
 */
static NodeHandlerFT _mfd_ipAddressPrefixTable_pre_request;
static NodeHandlerFT _mfd_ipAddressPrefixTable_post_request;
static NodeHandlerFT _mfd_ipAddressPrefixTable_object_lookup;
static NodeHandlerFT _mfd_ipAddressPrefixTable_get_values;
/**
 * @internal
 * Initialize the table ipAddressPrefixTable 
 *    (Define its contents and how it's structured)
 */
void _ipAddressPrefixTable_initialize_interface( ipAddressPrefixTable_registration* reg_ptr, u_long flags )
{
    BabyStepsAccessMethods* access_multiplexer = &ipAddressPrefixTable_if_ctx.access_multiplexer;
    TableRegistrationInfo* tbl_info = &ipAddressPrefixTable_if_ctx.tbl_info;
    HandlerRegistration* reginfo;
    MibHandler* handler;
    int mfd_modes = 0;

    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:_ipAddressPrefixTable_initialize_interface", "called\n" ) );

    /*************************************************
     *
     * save interface context for ipAddressPrefixTable
     */
    /*
     * Setting up the table's definition
     */
    Table_helperAddIndexes( tbl_info, ASN01_INTEGER,
        /** index: ipAddressPrefixIfIndex */
        ASN01_INTEGER,
        /** index: ipAddressPrefixType */
        ASN01_OCTET_STR,
        /** index: ipAddressPrefixPrefix */
        ASN01_UNSIGNED,
        /** index: ipAddressPrefixLength */
        0 );

    /*
     * Define the minimum and maximum accessible columns.  This
     * optimizes retrieval. 
     */
    tbl_info->min_column = IPADDRESSPREFIXTABLE_MIN_COL;
    tbl_info->max_column = IPADDRESSPREFIXTABLE_MAX_COL;

    /*
     * save users context
     */
    ipAddressPrefixTable_if_ctx.user_ctx = reg_ptr;

    /*
     * call data access initialization code
     */
    ipAddressPrefixTable_init_data( reg_ptr );

    /*
     * set up the container
     */
    _ipAddressPrefixTable_container_init( &ipAddressPrefixTable_if_ctx );
    if ( NULL == ipAddressPrefixTable_if_ctx.container ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "could not initialize container for ipAddressPrefixTable\n" );
        return;
    }

    /*
     * access_multiplexer: REQUIRED wrapper for get request handling
     */
    access_multiplexer->object_lookup = _mfd_ipAddressPrefixTable_object_lookup;
    access_multiplexer->get_values = _mfd_ipAddressPrefixTable_get_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->pre_request = _mfd_ipAddressPrefixTable_pre_request;
    access_multiplexer->post_request = _mfd_ipAddressPrefixTable_post_request;

    /*************************************************
     *
     * Create a registration, save our reg data, register table.
     */
    DEBUG_MSGTL( ( "ipAddressPrefixTable:init_ipAddressPrefixTable",
        "Registering ipAddressPrefixTable as a mibs-for-dummies table.\n" ) );
    handler = BabySteps_accessMultiplexerGet( access_multiplexer );
    reginfo = AgentHandler_registrationCreate( "ipAddressPrefixTable",
        handler,
        ipAddressPrefixTable_oid,
        ipAddressPrefixTable_oid_size,
        HANDLER_CAN_BABY_STEP | HANDLER_CAN_RONLY );
    if ( NULL == reginfo ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "error registering table ipAddressPrefixTable\n" );
        return;
    }
    reginfo->my_reg_void = &ipAddressPrefixTable_if_ctx;

    /*************************************************
     *
     * set up baby steps handler, create it and inject it
     */
    if ( access_multiplexer->object_lookup )
        mfd_modes |= BabyStepsMode_OBJECT_LOOKUP;
    if ( access_multiplexer->pre_request )
        mfd_modes |= BabyStepsMode_PRE_REQUEST;
    if ( access_multiplexer->post_request )
        mfd_modes |= BabyStepsMode_POST_REQUEST;

    /* XXX - are these actually necessary? */
    if ( access_multiplexer->set_values )
        mfd_modes |= BabyStepsMode_SET_VALUES;
    if ( access_multiplexer->irreversible_commit )
        mfd_modes |= BabyStepsMode_IRREVERSIBLE_COMMIT;
    if ( access_multiplexer->object_syntax_checks )
        mfd_modes |= BabyStepsMode_CHECK_OBJECT;

    if ( access_multiplexer->undo_setup )
        mfd_modes |= BabyStepsMode_UNDO_SETUP;
    if ( access_multiplexer->undo_cleanup )
        mfd_modes |= BabyStepsMode_UNDO_CLEANUP;
    if ( access_multiplexer->undo_sets )
        mfd_modes |= BabyStepsMode_UNDO_SETS;

    if ( access_multiplexer->row_creation )
        mfd_modes |= BabyStepsMode_ROW_CREATE;
    if ( access_multiplexer->consistency_checks )
        mfd_modes |= BabyStepsMode_CHECK_CONSISTENCY;
    if ( access_multiplexer->commit )
        mfd_modes |= BabyStepsMode_COMMIT;
    if ( access_multiplexer->undo_commit )
        mfd_modes |= BabyStepsMode_UNDO_COMMIT;

    handler = BabySteps_handlerGet( mfd_modes );
    AgentHandler_injectHandler( reginfo, handler );

    /*************************************************
     *
     * inject row_merge helper with prefix rootoid_len + 2 (entry.col)
     */
    handler = RowMerge_getRowMergeHandler( reginfo->rootoid_len + 2 );
    AgentHandler_injectHandler( reginfo, handler );

    /*************************************************
     *
     * inject container_table helper
     */
    handler = TableContainer_handlerGet( tbl_info,
        ipAddressPrefixTable_if_ctx.container,
        TABLE_CONTAINER_KEY_NETSNMP_INDEX );
    AgentHandler_injectHandler( reginfo, handler );

    /*************************************************
     *
     * inject cache helper
     */
    if ( NULL != ipAddressPrefixTable_if_ctx.cache ) {
        handler = CacheHandler_handlerGet( ipAddressPrefixTable_if_ctx.cache );
        AgentHandler_injectHandler( reginfo, handler );
    }

    /*
     * register table
     */
    Table_registerTable( reginfo, tbl_info );

} /* _ipAddressPrefixTable_initialize_interface */

/**
 * @internal
 * Shutdown the table ipAddressPrefixTable
 */
void _ipAddressPrefixTable_shutdown_interface( ipAddressPrefixTable_registration* reg_ptr )
{
    /*
     * shutdown the container
     */
    _ipAddressPrefixTable_container_shutdown( &ipAddressPrefixTable_if_ctx );
}

void ipAddressPrefixTable_valid_columns_set( ColumnInfo* vc )
{
    ipAddressPrefixTable_if_ctx.tbl_info.valid_columns = vc;
} /* ipAddressPrefixTable_valid_columns_set */

/**
 * @internal
 * convert the index component stored in the context to an oid
 */
int ipAddressPrefixTable_index_to_oid( Types_Index* oid_idx,
    ipAddressPrefixTable_mib_index* mib_idx )
{
    int err = PRIOT_ERR_NOERROR;

    /*
     * temp storage for parsing indexes
     */
    /*
     * ipAddressPrefixIfIndex(1)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H
     */
    Types_VariableList var_ipAddressPrefixIfIndex;
    /*
     * ipAddressPrefixType(2)/InetAddressType/ASN_INTEGER/long(u_long)//l/a/w/E/r/d/h
     */
    Types_VariableList var_ipAddressPrefixType;
    /*
     * ipAddressPrefixPrefix(3)/InetAddress/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h
     */
    Types_VariableList var_ipAddressPrefixPrefix;
    /*
     * ipAddressPrefixLength(4)/InetAddressPrefixLength/ASN_UNSIGNED/u_long(u_long)//l/a/w/e/R/d/H
     */
    Types_VariableList var_ipAddressPrefixLength;

    /*
     * set up varbinds
     */
    memset( &var_ipAddressPrefixIfIndex, 0x00,
        sizeof( var_ipAddressPrefixIfIndex ) );
    var_ipAddressPrefixIfIndex.type = ASN01_INTEGER;
    memset( &var_ipAddressPrefixType, 0x00,
        sizeof( var_ipAddressPrefixType ) );
    var_ipAddressPrefixType.type = ASN01_INTEGER;
    memset( &var_ipAddressPrefixPrefix, 0x00,
        sizeof( var_ipAddressPrefixPrefix ) );
    var_ipAddressPrefixPrefix.type = ASN01_OCTET_STR;
    memset( &var_ipAddressPrefixLength, 0x00,
        sizeof( var_ipAddressPrefixLength ) );
    var_ipAddressPrefixLength.type = ASN01_UNSIGNED;

    /*
     * chain temp index varbinds together
     */
    var_ipAddressPrefixIfIndex.nextVariable = &var_ipAddressPrefixType;
    var_ipAddressPrefixType.nextVariable = &var_ipAddressPrefixPrefix;
    var_ipAddressPrefixPrefix.nextVariable = &var_ipAddressPrefixLength;
    var_ipAddressPrefixLength.nextVariable = NULL;

    DEBUG_MSGTL( ( "verbose:ipAddressPrefixTable:ipAddressPrefixTable_index_to_oid", "called\n" ) );

    /*
     * ipAddressPrefixIfIndex(1)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H 
     */
    Client_setVarValue( &var_ipAddressPrefixIfIndex,
        ( u_char* )&mib_idx->ipAddressPrefixIfIndex,
        sizeof( mib_idx->ipAddressPrefixIfIndex ) );

    /*
     * ipAddressPrefixType(2)/InetAddressType/ASN_INTEGER/long(u_long)//l/a/w/E/r/d/h 
     */
    Client_setVarValue( &var_ipAddressPrefixType,
        ( u_char* )&mib_idx->ipAddressPrefixType,
        sizeof( mib_idx->ipAddressPrefixType ) );

    /*
     * ipAddressPrefixPrefix(3)/InetAddress/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h 
     */
    Client_setVarValue( &var_ipAddressPrefixPrefix,
        ( u_char* )&mib_idx->ipAddressPrefixPrefix,
        mib_idx->ipAddressPrefixPrefix_len * sizeof( mib_idx->ipAddressPrefixPrefix[ 0 ] ) );

    /*
     * ipAddressPrefixLength(4)/InetAddressPrefixLength/ASN_UNSIGNED/u_long(u_long)//l/a/w/e/R/d/H 
     */
    Client_setVarValue( &var_ipAddressPrefixLength,
        ( u_char* )&mib_idx->ipAddressPrefixLength,
        sizeof( mib_idx->ipAddressPrefixLength ) );

    err = Mib_buildOidNoalloc( oid_idx->oids, oid_idx->len, &oid_idx->len,
        NULL, 0, &var_ipAddressPrefixIfIndex );
    if ( err )
        Logger_log( LOGGER_PRIORITY_ERR, "error %d converting index to oid\n", err );

    /*
     * parsing may have allocated memory. free it.
     */
    Client_resetVarBuffers( &var_ipAddressPrefixIfIndex );

    return err;
} /* ipAddressPrefixTable_index_to_oid */

/**
 * extract ipAddressPrefixTable indexes from a Types_Index
 *
 * @retval PRIOT_ERR_NOERROR  : no error
 * @retval PRIOT_ERR_GENERR   : error
 */
int ipAddressPrefixTable_index_from_oid( Types_Index* oid_idx,
    ipAddressPrefixTable_mib_index*
        mib_idx )
{
    int err = PRIOT_ERR_NOERROR;

    /*
     * temp storage for parsing indexes
     */
    /*
     * ipAddressPrefixIfIndex(1)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H
     */
    Types_VariableList var_ipAddressPrefixIfIndex;
    /*
     * ipAddressPrefixType(2)/InetAddressType/ASN_INTEGER/long(u_long)//l/a/w/E/r/d/h
     */
    Types_VariableList var_ipAddressPrefixType;
    /*
     * ipAddressPrefixPrefix(3)/InetAddress/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h
     */
    Types_VariableList var_ipAddressPrefixPrefix;
    /*
     * ipAddressPrefixLength(4)/InetAddressPrefixLength/ASN_UNSIGNED/u_long(u_long)//l/a/w/e/R/d/H
     */
    Types_VariableList var_ipAddressPrefixLength;

    /*
     * set up varbinds
     */
    memset( &var_ipAddressPrefixIfIndex, 0x00,
        sizeof( var_ipAddressPrefixIfIndex ) );
    var_ipAddressPrefixIfIndex.type = ASN01_INTEGER;
    memset( &var_ipAddressPrefixType, 0x00,
        sizeof( var_ipAddressPrefixType ) );
    var_ipAddressPrefixType.type = ASN01_INTEGER;
    memset( &var_ipAddressPrefixPrefix, 0x00,
        sizeof( var_ipAddressPrefixPrefix ) );
    var_ipAddressPrefixPrefix.type = ASN01_OCTET_STR;
    memset( &var_ipAddressPrefixLength, 0x00,
        sizeof( var_ipAddressPrefixLength ) );
    var_ipAddressPrefixLength.type = ASN01_UNSIGNED;

    /*
     * chain temp index varbinds together
     */
    var_ipAddressPrefixIfIndex.nextVariable = &var_ipAddressPrefixType;
    var_ipAddressPrefixType.nextVariable = &var_ipAddressPrefixPrefix;
    var_ipAddressPrefixPrefix.nextVariable = &var_ipAddressPrefixLength;
    var_ipAddressPrefixLength.nextVariable = NULL;

    DEBUG_MSGTL( ( "verbose:ipAddressPrefixTable:ipAddressPrefixTable_index_from_oid", "called\n" ) );

    /*
     * parse the oid into the individual index components
     */
    err = Mib_parseOidIndexes( oid_idx->oids, oid_idx->len,
        &var_ipAddressPrefixIfIndex );
    if ( err == PRIOT_ERR_NOERROR ) {
        /*
         * copy out values
         */
        mib_idx->ipAddressPrefixIfIndex = *( ( long* )var_ipAddressPrefixIfIndex.val.string );
        mib_idx->ipAddressPrefixType = *( ( u_long* )var_ipAddressPrefixType.val.string );
        /*
         * NOTE: val_len is in bytes, ipAddressPrefixPrefix_len might not be
         */
        if ( var_ipAddressPrefixPrefix.valLen > sizeof( mib_idx->ipAddressPrefixPrefix ) )
            err = PRIOT_ERR_GENERR;
        else {
            memcpy( mib_idx->ipAddressPrefixPrefix,
                var_ipAddressPrefixPrefix.val.string,
                var_ipAddressPrefixPrefix.valLen );
            mib_idx->ipAddressPrefixPrefix_len = var_ipAddressPrefixPrefix.valLen / sizeof( mib_idx->ipAddressPrefixPrefix[ 0 ] );
        }
        mib_idx->ipAddressPrefixLength = *( ( u_long* )var_ipAddressPrefixLength.val.string );
    }

    /*
     * parsing may have allocated memory. free it.
     */
    Client_resetVarBuffers( &var_ipAddressPrefixIfIndex );

    return err;
} /* ipAddressPrefixTable_index_from_oid */

/*
 *********************************************************************
 * @internal
 * allocate resources for a ipAddressPrefixTable_rowreq_ctx
 */
ipAddressPrefixTable_rowreq_ctx*
ipAddressPrefixTable_allocate_rowreq_ctx( void* user_init_ctx )
{
    ipAddressPrefixTable_rowreq_ctx* rowreq_ctx = TOOLS_MALLOC_TYPEDEF( ipAddressPrefixTable_rowreq_ctx );

    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:ipAddressPrefixTable_allocate_rowreq_ctx", "called\n" ) );

    if ( NULL == rowreq_ctx ) {
        Logger_log( LOGGER_PRIORITY_ERR, "Couldn't allocate memory for a "
                                         "ipAddressPrefixTable_rowreq_ctx.\n" );
        return NULL;
    }

    rowreq_ctx->oid_idx.oids = rowreq_ctx->oid_tmp;

    rowreq_ctx->ipAddressPrefixTable_data_list = NULL;

    /*
     * if we allocated data, call init routine
     */
    if ( !( rowreq_ctx->rowreq_flags & MFD_ROW_DATA_FROM_USER ) ) {
        if ( ErrorCode_SUCCESS != ipAddressPrefixTable_rowreq_ctx_init( rowreq_ctx,
                                      user_init_ctx ) ) {
            ipAddressPrefixTable_release_rowreq_ctx( rowreq_ctx );
            rowreq_ctx = NULL;
        }
    }

    return rowreq_ctx;
} /* ipAddressPrefixTable_allocate_rowreq_ctx */

/*
 * @internal
 * release resources for a ipAddressPrefixTable_rowreq_ctx
 */
void ipAddressPrefixTable_release_rowreq_ctx( ipAddressPrefixTable_rowreq_ctx*
        rowreq_ctx )
{
    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:ipAddressPrefixTable_release_rowreq_ctx", "called\n" ) );

    Assert_assert( NULL != rowreq_ctx );

    ipAddressPrefixTable_rowreq_ctx_cleanup( rowreq_ctx );

    /*
     * free index oid pointer
     */
    if ( rowreq_ctx->oid_idx.oids != rowreq_ctx->oid_tmp )
        free( rowreq_ctx->oid_idx.oids );

    TOOLS_FREE( rowreq_ctx );
} /* ipAddressPrefixTable_release_rowreq_ctx */

/**
 * @internal
 * wrapper
 */
static int
_mfd_ipAddressPrefixTable_pre_request( MibHandler* handler, HandlerRegistration* reginfo, AgentRequestInfo* agtreq_info,
    RequestInfo* requests )
{
    int rc;

    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:_mfd_ipAddressPrefixTable_pre_request", "called\n" ) );

    if ( 1 != RowMerge_statusFirst( reginfo, agtreq_info ) ) {
        DEBUG_MSGTL( ( "internal:ipAddressPrefixTable",
            "skipping additional pre_request\n" ) );
        return PRIOT_ERR_NOERROR;
    }

    rc = ipAddressPrefixTable_pre_request( ipAddressPrefixTable_if_ctx.user_ctx );
    if ( MFD_SUCCESS != rc ) {
        /*
         * nothing we can do about it but log it
         */
        DEBUG_MSGTL( ( "ipAddressPrefixTable", "error %d from "
                                               "ipAddressPrefixTable_pre_request\n",
            rc ) );
        Agent_requestSetErrorAll( requests, PRIOT_VALIDATE_ERR( rc ) );
    }

    return PRIOT_ERR_NOERROR;
} /* _mfd_ipAddressPrefixTable_pre_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_ipAddressPrefixTable_post_request( MibHandler* handler, HandlerRegistration* reginfo, AgentRequestInfo* agtreq_info,
    RequestInfo* requests )
{
    ipAddressPrefixTable_rowreq_ctx* rowreq_ctx = ( ipAddressPrefixTable_rowreq_ctx* )
        TableContainer_rowExtract( requests );
    int rc, packet_rc;

    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:_mfd_ipAddressPrefixTable_post_request", "called\n" ) );

    /*
     * release row context, if deleted
     */
    if ( rowreq_ctx && ( rowreq_ctx->rowreq_flags & MFD_ROW_DELETED ) )
        ipAddressPrefixTable_release_rowreq_ctx( rowreq_ctx );

    /*
     * wait for last call before calling user
     */
    if ( 1 != RowMerge_statusLast( reginfo, agtreq_info ) ) {
        DEBUG_MSGTL( ( "internal:ipAddressPrefixTable",
            "waiting for last post_request\n" ) );
        return PRIOT_ERR_NOERROR;
    }

    packet_rc = Agent_checkAllRequestsError( agtreq_info->asp, 0 );
    rc = ipAddressPrefixTable_post_request( ipAddressPrefixTable_if_ctx.user_ctx, packet_rc );
    if ( MFD_SUCCESS != rc ) {
        /*
         * nothing we can do about it but log it
         */
        DEBUG_MSGTL( ( "ipAddressPrefixTable", "error %d from "
                                               "ipAddressPrefixTable_post_request\n",
            rc ) );
    }

    return PRIOT_ERR_NOERROR;
} /* _mfd_ipAddressPrefixTable_post_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_ipAddressPrefixTable_object_lookup( MibHandler* handler, HandlerRegistration* reginfo, AgentRequestInfo* agtreq_info,
    RequestInfo* requests )
{
    int rc = PRIOT_ERR_NOERROR;
    ipAddressPrefixTable_rowreq_ctx* rowreq_ctx = ( ipAddressPrefixTable_rowreq_ctx* )
        TableContainer_rowExtract( requests );

    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:_mfd_ipAddressPrefixTable_object_lookup", "called\n" ) );

    /*
     * get our context from mfd
     * ipAddressPrefixTable_interface_ctx *if_ctx =
     *             (ipAddressPrefixTable_interface_ctx *)reginfo->my_reg_void;
     */

    if ( NULL == rowreq_ctx ) {
        rc = PRIOT_ERR_NOCREATION;
    }

    if ( MFD_SUCCESS != rc )
        Agent_requestSetErrorAll( requests, rc );
    else
        ipAddressPrefixTable_row_prep( rowreq_ctx );

    return PRIOT_VALIDATE_ERR( rc );
} /* _mfd_ipAddressPrefixTable_object_lookup */

/***********************************************************************
 *
 * GET processing
 *
 ***********************************************************************/
/*
 * @internal
 * Retrieve the value for a particular column
 */
static inline int
_ipAddressPrefixTable_get_column( ipAddressPrefixTable_rowreq_ctx*
                                      rowreq_ctx,
    Types_VariableList* var,
    int column )
{
    int rc = ErrorCode_SUCCESS;

    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:_mfd_ipAddressPrefixTable_get_column", "called for %d\n", column ) );

    Assert_assert( NULL != rowreq_ctx );

    switch ( column ) {

    /*
         * ipAddressPrefixOrigin(5)/IpAddressPrefixOriginTC/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h 
         */
    case COLUMN_IPADDRESSPREFIXORIGIN:
        var->valLen = sizeof( u_long );
        var->type = ASN01_INTEGER;
        rc = ipAddressPrefixOrigin_get( rowreq_ctx,
            ( u_long* )var->val.string );
        break;

    /*
         * ipAddressPrefixOnLinkFlag(6)/TruthValue/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h 
         */
    case COLUMN_IPADDRESSPREFIXONLINKFLAG:
        var->valLen = sizeof( u_long );
        var->type = ASN01_INTEGER;
        rc = ipAddressPrefixOnLinkFlag_get( rowreq_ctx,
            ( u_long* )var->val.string );
        break;

    /*
         * ipAddressPrefixAutonomousFlag(7)/TruthValue/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h 
         */
    case COLUMN_IPADDRESSPREFIXAUTONOMOUSFLAG:
        var->valLen = sizeof( u_long );
        var->type = ASN01_INTEGER;
        rc = ipAddressPrefixAutonomousFlag_get( rowreq_ctx,
            ( u_long* )var->val.string );
        break;

    /*
         * ipAddressPrefixAdvPreferredLifetime(8)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPADDRESSPREFIXADVPREFERREDLIFETIME:
        var->valLen = sizeof( u_long );
        var->type = ASN01_UNSIGNED;
        rc = ipAddressPrefixAdvPreferredLifetime_get( rowreq_ctx,
            ( u_long* )var->val.string );
        break;

    /*
         * ipAddressPrefixAdvValidLifetime(9)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPADDRESSPREFIXADVVALIDLIFETIME:
        var->valLen = sizeof( u_long );
        var->type = ASN01_UNSIGNED;
        rc = ipAddressPrefixAdvValidLifetime_get( rowreq_ctx,
            ( u_long* )var->val.string );
        break;

    default:
        Logger_log( LOGGER_PRIORITY_ERR,
            "unknown column %d in _ipAddressPrefixTable_get_column\n",
            column );
        break;
    }

    return rc;
} /* _ipAddressPrefixTable_get_column */

int _mfd_ipAddressPrefixTable_get_values( MibHandler* handler,
    HandlerRegistration* reginfo,
    AgentRequestInfo* agtreq_info,
    RequestInfo* requests )
{
    ipAddressPrefixTable_rowreq_ctx* rowreq_ctx = ( ipAddressPrefixTable_rowreq_ctx* )
        TableContainer_rowExtract( requests );
    TableRequestInfo* tri;
    u_char* old_string;
    void ( *dataFreeHook )( void* );
    int rc;

    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:_mfd_ipAddressPrefixTable_get_values", "called\n" ) );

    Assert_assert( NULL != rowreq_ctx );

    for ( ; requests; requests = requests->next ) {
        /*
         * save old pointer, so we can free it if replaced
         */
        old_string = requests->requestvb->val.string;
        dataFreeHook = requests->requestvb->dataFreeHook;
        if ( NULL == requests->requestvb->val.string ) {
            requests->requestvb->val.string = requests->requestvb->buf;
            requests->requestvb->valLen = sizeof( requests->requestvb->buf );
        } else if ( requests->requestvb->buf == requests->requestvb->val.string ) {
            if ( requests->requestvb->valLen != sizeof( requests->requestvb->buf ) )
                requests->requestvb->valLen = sizeof( requests->requestvb->buf );
        }

        /*
         * get column data
         */
        tri = Table_extractTableInfo( requests );
        if ( NULL == tri )
            continue;

        rc = _ipAddressPrefixTable_get_column( rowreq_ctx,
            requests->requestvb,
            tri->colnum );
        if ( rc ) {
            if ( MFD_SKIP == rc ) {
                requests->requestvb->type = PRIOT_NOSUCHINSTANCE;
                rc = PRIOT_ERR_NOERROR;
            }
        } else if ( NULL == requests->requestvb->val.string ) {
            Logger_log( LOGGER_PRIORITY_ERR, "NULL varbind data pointer!\n" );
            rc = PRIOT_ERR_GENERR;
        }
        if ( rc )
            Agent_requestSetError( requests, PRIOT_VALIDATE_ERR( rc ) );

        /*
         * if the buffer wasn't used previously for the old data (i.e. it
         * was allcoated memory)  and the get routine replaced the pointer,
         * we need to free the previous pointer.
         */
        if ( old_string && ( old_string != requests->requestvb->buf ) && ( requests->requestvb->val.string != old_string ) ) {
            if ( dataFreeHook )
                ( *dataFreeHook )( old_string );
            else
                free( old_string );
        }
    } /* for results */

    return PRIOT_ERR_NOERROR;
} /* _mfd_ipAddressPrefixTable_get_values */

/***********************************************************************
 *
 * SET processing
 *
 ***********************************************************************/

/*
 * SET PROCESSING NOT APPLICABLE (per MIB or user setting)
 */

/***********************************************************************
 *
 * DATA ACCESS
 *
 ***********************************************************************/
static void _container_free( Container_Container* container );

/**
 * @internal
 */
static int
_cache_load( Cache* cache, void* vmagic )
{
    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:_cache_load", "called\n" ) );

    if ( ( NULL == cache ) || ( NULL == cache->magic ) ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "invalid cache for ipAddressPrefixTable_cache_load\n" );
        return -1;
    }

    /** should only be called for an invalid or expired cache */
    Assert_assert( ( 0 == cache->valid ) || ( 1 == cache->expired ) );

    /*
     * call user code
     */
    return ipAddressPrefixTable_container_load( ( Container_Container* )
                                                    cache->magic );
} /* _cache_load */

/**
 * @internal
 */
static void
_cache_free( Cache* cache, void* magic )
{
    Container_Container* container;

    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:_cache_free", "called\n" ) );

    if ( ( NULL == cache ) || ( NULL == cache->magic ) ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "invalid cache in ipAddressPrefixTable_cache_free\n" );
        return;
    }

    container = ( Container_Container* )cache->magic;

    _container_free( container );
} /* _cache_free */

/**
 * @internal
 */
static void
_container_item_free( ipAddressPrefixTable_rowreq_ctx* rowreq_ctx,
    void* context )
{
    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:_container_item_free",
        "called\n" ) );

    if ( NULL == rowreq_ctx )
        return;

    ipAddressPrefixTable_release_rowreq_ctx( rowreq_ctx );
} /* _container_item_free */

/**
 * @internal
 */
static void
_container_free( Container_Container* container )
{
    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:_container_free",
        "called\n" ) );

    if ( NULL == container ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "invalid container in ipAddressPrefixTable_container_free\n" );
        return;
    }

    /*
     * call user code
     */
    ipAddressPrefixTable_container_free( container );

    /*
     * free all items. inefficient, but easy.
     */
    CONTAINER_CLEAR( container,
        ( Container_FuncObjFunc* )_container_item_free,
        NULL );
} /* _container_free */

/**
 * @internal
 * initialize the container with functions or wrappers
 */
void _ipAddressPrefixTable_container_init( ipAddressPrefixTable_interface_ctx*
        if_ctx )
{
    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:_ipAddressPrefixTable_container_init", "called\n" ) );

    /*
     * cache init
     */
    if_ctx->cache = CacheHandler_create( 30, /* timeout in seconds */
        _cache_load, _cache_free,
        ipAddressPrefixTable_oid,
        ipAddressPrefixTable_oid_size );

    if ( NULL == if_ctx->cache ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "error creating cache for ipAddressPrefixTable\n" );
        return;
    }

    if_ctx->cache->flags = CacheOperation_DONT_INVALIDATE_ON_SET;

    ipAddressPrefixTable_container_init( &if_ctx->container, if_ctx->cache );
    if ( NULL == if_ctx->container )
        if_ctx->container = Container_find( "ipAddressPrefixTable:tableContainer" );
    if ( NULL == if_ctx->container ) {
        Logger_log( LOGGER_PRIORITY_ERR, "error creating container in "
                                         "ipAddressPrefixTable_container_init\n" );
        return;
    }

    if ( NULL != if_ctx->cache )
        if_ctx->cache->magic = ( void* )if_ctx->container;
} /* _ipAddressPrefixTable_container_init */

/**
 * @internal
 * shutdown the container with functions or wrappers
 */
void _ipAddressPrefixTable_container_shutdown( ipAddressPrefixTable_interface_ctx* if_ctx )
{
    DEBUG_MSGTL( ( "internal:ipAddressPrefixTable:_ipAddressPrefixTable_container_shutdown", "called\n" ) );

    ipAddressPrefixTable_container_shutdown( if_ctx->container );

    _container_free( if_ctx->container );

} /* _ipAddressPrefixTable_container_shutdown */

ipAddressPrefixTable_rowreq_ctx*
ipAddressPrefixTable_row_find_by_mib_index( ipAddressPrefixTable_mib_index*
        mib_idx )
{
    ipAddressPrefixTable_rowreq_ctx* rowreq_ctx;
    oid oid_tmp[ ASN01_MAX_OID_LEN ];
    Types_Index oid_idx;
    int rc;

    /*
     * set up storage for OID
     */
    oid_idx.oids = oid_tmp;
    oid_idx.len = sizeof( oid_tmp ) / sizeof( oid );

    /*
     * convert
     */
    rc = ipAddressPrefixTable_index_to_oid( &oid_idx, mib_idx );
    if ( MFD_SUCCESS != rc )
        return NULL;

    rowreq_ctx = ( ipAddressPrefixTable_rowreq_ctx* )
        CONTAINER_FIND( ipAddressPrefixTable_if_ctx.container, &oid_idx );

    return rowreq_ctx;
}
