/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 15899 $ of $ 
 *
 * $Id:$
 */
/*
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 * ***                                                               ***
 * ***  NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE  ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THIS FILE DOES NOT CONTAIN ANY USER EDITABLE CODE.      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THE GENERATED CODE IS INTERNAL IMPLEMENTATION, AND      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***    IS SUBJECT TO CHANGE WITHOUT WARNING IN FUTURE RELEASES.   ***
 * ***                                                               ***
 * ***                                                               ***
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 */

#include "ipIfStatsTable_interface.h"
#include "System/Util/Assert.h"
#include "BabySteps.h"
#include "CacheHandler.h"
#include "Client.h"
#include "System/Util/Trace.h"
#include "System/Util/Logger.h"
#include "Mib.h"
#include "RowMerge.h"
#include "TableContainer.h"
#include "Watcher.h"
#include "ipIfStatsTable_data_access.h"
#include "ipIfStatsTable_data_get.h"
#include "ipIfStatsTable_oids.h"
#include "siglog/agent/mfd.h"

/**********************************************************************
 **********************************************************************
 ***
 *** Table ipIfStatsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * IP-MIB::ipIfStatsTable is subid 3 of ipTrafficStats.
 * Its status is Current.
 * OID: .1.3.6.1.2.1.4.31.3, length: 9
 */
typedef struct ipIfStatsTable_interface_ctx_s {

    Container_Container* container;
    Cache* cache;

    ipIfStatsTable_registration* user_ctx;

    TableRegistrationInfo tbl_info;

    BabyStepsAccessMethods access_multiplexer;

    u_long last_changed;

} ipIfStatsTable_interface_ctx;

static ipIfStatsTable_interface_ctx ipIfStatsTable_if_ctx;

static void _ipIfStatsTable_container_init( ipIfStatsTable_interface_ctx* if_ctx );
static void
_ipIfStatsTable_container_shutdown( ipIfStatsTable_interface_ctx* if_ctx );

Container_Container*
ipIfStatsTable_container_get( void )
{
    return ipIfStatsTable_if_ctx.container;
}

ipIfStatsTable_registration*
ipIfStatsTable_registration_get( void )
{
    return ipIfStatsTable_if_ctx.user_ctx;
}

ipIfStatsTable_registration*
ipIfStatsTable_registration_set( ipIfStatsTable_registration* newreg )
{
    ipIfStatsTable_registration* old = ipIfStatsTable_if_ctx.user_ctx;
    ipIfStatsTable_if_ctx.user_ctx = newreg;
    return old;
}

int ipIfStatsTable_container_size( void )
{
    return CONTAINER_SIZE( ipIfStatsTable_if_ctx.container );
}

/*
 * ipIfStatsTableLastChanged, which is the last time that a row in
 * the table was changed or the last time a row was added/deleted from the
 * table.
 */
void ipIfStatsTable_lastChange_set( u_long table_changed )
{
    DEBUG_MSGTL( ( "ipIfStatsTable:lastChanged_set",
        "called. was %ld, now %ld\n",
        ipIfStatsTable_if_ctx.last_changed, table_changed ) );
    ipIfStatsTable_if_ctx.last_changed = table_changed;
}

/*
 * mfd multiplexer modes
 */
static NodeHandlerFT _mfd_ipIfStatsTable_pre_request;
static NodeHandlerFT _mfd_ipIfStatsTable_post_request;
static NodeHandlerFT _mfd_ipIfStatsTable_object_lookup;
static NodeHandlerFT _mfd_ipIfStatsTable_get_values;
/**
 * @internal
 * Initialize the table ipIfStatsTable 
 *    (Define its contents and how it's structured)
 */
void _ipIfStatsTable_initialize_interface( ipIfStatsTable_registration* reg_ptr,
    u_long flags )
{
    BabyStepsAccessMethods* access_multiplexer = &ipIfStatsTable_if_ctx.access_multiplexer;
    TableRegistrationInfo* tbl_info = &ipIfStatsTable_if_ctx.tbl_info;
    HandlerRegistration* reginfo;
    MibHandler* handler;
    int mfd_modes = 0;

    DEBUG_MSGTL( ( "internal:ipIfStatsTable:_ipIfStatsTable_initialize_interface", "called\n" ) );

    /*************************************************
     *
     * save interface context for ipIfStatsTable
     */
    /*
     * Setting up the table's definition
     */
    Table_helperAddIndexes( tbl_info, asnINTEGER,
        /** index: ipIfStatsIPVersion */
        asnINTEGER,
        /** index: ipIfStatsIfIndex */
        0 );

    /*
     * Define the minimum and maximum accessible columns.  This
     * optimizes retrieval. 
     */
    tbl_info->min_column = IPIFSTATSTABLE_MIN_COL;
    tbl_info->max_column = IPIFSTATSTABLE_MAX_COL;

    /*
     * save users context
     */
    ipIfStatsTable_if_ctx.user_ctx = reg_ptr;

    /*
     * call data access initialization code
     */
    ipIfStatsTable_init_data( reg_ptr );

    /*
     * set up the container
     */
    _ipIfStatsTable_container_init( &ipIfStatsTable_if_ctx );
    if ( NULL == ipIfStatsTable_if_ctx.container ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "could not initialize container for ipIfStatsTable\n" );
        return;
    }

    /*
     * access_multiplexer: REQUIRED wrapper for get request handling
     */
    access_multiplexer->object_lookup = _mfd_ipIfStatsTable_object_lookup;
    access_multiplexer->get_values = _mfd_ipIfStatsTable_get_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->pre_request = _mfd_ipIfStatsTable_pre_request;
    access_multiplexer->post_request = _mfd_ipIfStatsTable_post_request;

    /*************************************************
     *
     * Create a registration, save our reg data, register table.
     */
    DEBUG_MSGTL( ( "ipIfStatsTable:init_ipIfStatsTable",
        "Registering ipIfStatsTable as a mibs-for-dummies table.\n" ) );
    handler = BabySteps_accessMultiplexerGet( access_multiplexer );
    reginfo = AgentHandler_registrationCreate( "ipIfStatsTable", handler,
        ipIfStatsTable_oid,
        ipIfStatsTable_oid_size,
        HANDLER_CAN_BABY_STEP | HANDLER_CAN_RONLY );
    if ( NULL == reginfo ) {
        Logger_log( LOGGER_PRIORITY_ERR, "error registering table ipIfStatsTable\n" );
        return;
    }
    reginfo->my_reg_void = &ipIfStatsTable_if_ctx;

    /*************************************************
     *
     * set up baby steps handler, create it and inject it
     */
    if ( access_multiplexer->object_lookup )
        mfd_modes |= BabyStepsMode_OBJECT_LOOKUP;

    if ( access_multiplexer->pre_request )
        mfd_modes |= BabyStepsMode_PRE_REQUEST;
    if ( access_multiplexer->post_request )
        mfd_modes |= BabyStepsMode_POST_REQUEST;

    if ( access_multiplexer->set_values )
        mfd_modes |= BabyStepsMode_SET_VALUES;
    if ( access_multiplexer->irreversible_commit )
        mfd_modes |= BabyStepsMode_IRREVERSIBLE_COMMIT;
    if ( access_multiplexer->object_syntax_checks )
        mfd_modes |= BabyStepsMode_CHECK_OBJECT;

    if ( access_multiplexer->undo_setup )
        mfd_modes |= BabyStepsMode_UNDO_SETUP;
    if ( access_multiplexer->undo_cleanup )
        mfd_modes |= BabyStepsMode_UNDO_CLEANUP;
    if ( access_multiplexer->undo_sets )
        mfd_modes |= BabyStepsMode_UNDO_SETS;

    if ( access_multiplexer->row_creation )
        mfd_modes |= BabyStepsMode_ROW_CREATE;
    if ( access_multiplexer->consistency_checks )
        mfd_modes |= BabyStepsMode_CHECK_CONSISTENCY;
    if ( access_multiplexer->commit )
        mfd_modes |= BabyStepsMode_COMMIT;
    if ( access_multiplexer->undo_commit )
        mfd_modes |= BabyStepsMode_UNDO_COMMIT;

    handler = BabySteps_handlerGet( mfd_modes );
    AgentHandler_injectHandler( reginfo, handler );

    /*************************************************
     *
     * inject row_merge helper with prefix rootoid_len + 2 (entry.col)
     */
    handler = RowMerge_getRowMergeHandler( reginfo->rootoid_len + 2 );
    AgentHandler_injectHandler( reginfo, handler );

    /*************************************************
     *
     * inject container_table helper
     */
    handler = TableContainer_handlerGet( tbl_info,
        ipIfStatsTable_if_ctx.container,
        TABLE_CONTAINER_KEY_NETSNMP_INDEX );
    AgentHandler_injectHandler( reginfo, handler );

    /*************************************************
     *
     * inject cache helper
     */
    if ( NULL != ipIfStatsTable_if_ctx.cache ) {
        handler = CacheHandler_handlerGet( ipIfStatsTable_if_ctx.cache );
        AgentHandler_injectHandler( reginfo, handler );
    }

    /*
     * register table
     */
    Table_registerTable( reginfo, tbl_info );

    /*
     * register LastChanged
     */
    {
        oid lc_oid[] = { IPIFSTATSTABLELASTCHANGE_OID };
        Watcher_registerWatchedScalar2( AgentHandler_createHandlerRegistration( "ipIfStatsTableLastChanged", NULL,
                                            lc_oid, asnOID_LENGTH( lc_oid ),
                                            HANDLER_CAN_RONLY ),
            Watcher_createWatcherInfo( ( void* )&ipIfStatsTable_if_ctx.last_changed,
                                            sizeof( u_long ),
                                            asnTIMETICKS,
                                            WATCHER_FIXED_SIZE ) );
    }
} /* _ipIfStatsTable_initialize_interface */

/**
 * @internal
 * Shutdown the table ipIfStatsTable
 */
void _ipIfStatsTable_shutdown_interface( ipIfStatsTable_registration* reg_ptr )
{
    /*
     * shutdown the container
     */
    _ipIfStatsTable_container_shutdown( &ipIfStatsTable_if_ctx );
}

void ipIfStatsTable_valid_columns_set( ColumnInfo* vc )
{
    ipIfStatsTable_if_ctx.tbl_info.valid_columns = vc;
} /* ipIfStatsTable_valid_columns_set */

/**
 * @internal
 * convert the index component stored in the context to an oid
 */
int ipIfStatsTable_index_to_oid( Types_Index* oid_idx,
    ipIfStatsTable_mib_index* mib_idx )
{
    int err = PRIOT_ERR_NOERROR;

    /*
     * temp storage for parsing indexes
     */
    /*
     * ipIfStatsIPVersion(1)/InetVersion/ASN_INTEGER/long(u_long)//l/a/w/E/r/d/h
     */
    VariableList var_ipIfStatsIPVersion;
    /*
     * ipIfStatsIfIndex(2)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H
     */
    VariableList var_ipIfStatsIfIndex;

    /*
     * set up varbinds
     */
    memset( &var_ipIfStatsIPVersion, 0x00, sizeof( var_ipIfStatsIPVersion ) );
    var_ipIfStatsIPVersion.type = asnINTEGER;
    memset( &var_ipIfStatsIfIndex, 0x00, sizeof( var_ipIfStatsIfIndex ) );
    var_ipIfStatsIfIndex.type = asnINTEGER;

    /*
     * chain temp index varbinds together
     */
    var_ipIfStatsIPVersion.next = &var_ipIfStatsIfIndex;
    var_ipIfStatsIfIndex.next = NULL;

    DEBUG_MSGTL( ( "verbose:ipIfStatsTable:ipIfStatsTable_index_to_oid",
        "called\n" ) );

    /*
     * ipIfStatsIPVersion(1)/InetVersion/ASN_INTEGER/long(u_long)//l/a/w/E/r/d/h 
     */
    Client_setVarValue( &var_ipIfStatsIPVersion,
        ( u_char* )&mib_idx->ipIfStatsIPVersion,
        sizeof( mib_idx->ipIfStatsIPVersion ) );

    /*
     * ipIfStatsIfIndex(2)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H 
     */
    Client_setVarValue( &var_ipIfStatsIfIndex,
        ( u_char* )&mib_idx->ipIfStatsIfIndex,
        sizeof( mib_idx->ipIfStatsIfIndex ) );

    err = Mib_buildOidNoalloc( oid_idx->oids, oid_idx->len, &oid_idx->len,
        NULL, 0, &var_ipIfStatsIPVersion );
    if ( err )
        Logger_log( LOGGER_PRIORITY_ERR, "error %d converting index to oid\n", err );

    /*
     * parsing may have allocated memory. free it.
     */
    Client_resetVarBuffers( &var_ipIfStatsIPVersion );

    return err;
} /* ipIfStatsTable_index_to_oid */

/**
 * extract ipIfStatsTable indexes from a Types_Index
 *
 * @retval PRIOT_ERR_NOERROR  : no error
 * @retval PRIOT_ERR_GENERR   : error
 */
int ipIfStatsTable_index_from_oid( Types_Index* oid_idx,
    ipIfStatsTable_mib_index* mib_idx )
{
    int err = PRIOT_ERR_NOERROR;

    /*
     * temp storage for parsing indexes
     */
    /*
     * ipIfStatsIPVersion(1)/InetVersion/ASN_INTEGER/long(u_long)//l/a/w/E/r/d/h
     */
    VariableList var_ipIfStatsIPVersion;
    /*
     * ipIfStatsIfIndex(2)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H
     */
    VariableList var_ipIfStatsIfIndex;

    /*
     * set up varbinds
     */
    memset( &var_ipIfStatsIPVersion, 0x00, sizeof( var_ipIfStatsIPVersion ) );
    var_ipIfStatsIPVersion.type = asnINTEGER;
    memset( &var_ipIfStatsIfIndex, 0x00, sizeof( var_ipIfStatsIfIndex ) );
    var_ipIfStatsIfIndex.type = asnINTEGER;

    /*
     * chain temp index varbinds together
     */
    var_ipIfStatsIPVersion.next = &var_ipIfStatsIfIndex;
    var_ipIfStatsIfIndex.next = NULL;

    DEBUG_MSGTL( ( "verbose:ipIfStatsTable:ipIfStatsTable_index_from_oid",
        "called\n" ) );

    /*
     * parse the oid into the individual index components
     */
    err = Mib_parseOidIndexes( oid_idx->oids, oid_idx->len,
        &var_ipIfStatsIPVersion );
    if ( err == PRIOT_ERR_NOERROR ) {
        /*
         * copy out values
         */
        mib_idx->ipIfStatsIPVersion = *( ( u_long* )var_ipIfStatsIPVersion.value.string );
        mib_idx->ipIfStatsIfIndex = *( ( long* )var_ipIfStatsIfIndex.value.string );
    }

    /*
     * parsing may have allocated memory. free it.
     */
    Client_resetVarBuffers( &var_ipIfStatsIPVersion );

    return err;
} /* ipIfStatsTable_index_from_oid */

/*
 *********************************************************************
 * @internal
 * allocate resources for a ipIfStatsTable_rowreq_ctx
 */
ipIfStatsTable_rowreq_ctx*
ipIfStatsTable_allocate_rowreq_ctx( ipIfStatsTable_data* data,
    void* user_init_ctx )
{
    ipIfStatsTable_rowreq_ctx* rowreq_ctx = MEMORY_MALLOC_TYPEDEF( ipIfStatsTable_rowreq_ctx );

    DEBUG_MSGTL( ( "internal:ipIfStatsTable:ipIfStatsTable_allocate_rowreq_ctx", "called\n" ) );

    if ( NULL == rowreq_ctx ) {
        Logger_log( LOGGER_PRIORITY_ERR, "Couldn't allocate memory for a "
                                         "ipIfStatsTable_rowreq_ctx.\n" );
        return NULL;
    } else {
        if ( NULL != data ) {
            /*
             * track if we got data from user
             */
            rowreq_ctx->rowreq_flags |= MFD_ROW_DATA_FROM_USER;
            rowreq_ctx->data = data;
        } else if ( NULL == ( rowreq_ctx->data = ipIfStatsTable_allocate_data() ) ) {
            MEMORY_FREE( rowreq_ctx );
            return NULL;
        }
    }

    /*
     * undo context will be allocated when needed (in *_undo_setup)
     */

    rowreq_ctx->oid_idx.oids = rowreq_ctx->oid_tmp;

    rowreq_ctx->ipIfStatsTable_data_list = NULL;

    /*
     * if we allocated data, call init routine
     */
    if ( !( rowreq_ctx->rowreq_flags & MFD_ROW_DATA_FROM_USER ) ) {
        if ( ErrorCode_SUCCESS != ipIfStatsTable_rowreq_ctx_init( rowreq_ctx, user_init_ctx ) ) {
            ipIfStatsTable_release_rowreq_ctx( rowreq_ctx );
            rowreq_ctx = NULL;
        }
    }

    return rowreq_ctx;
} /* ipIfStatsTable_allocate_rowreq_ctx */

/*
 * @internal
 * release resources for a ipIfStatsTable_rowreq_ctx
 */
void ipIfStatsTable_release_rowreq_ctx( ipIfStatsTable_rowreq_ctx* rowreq_ctx )
{
    DEBUG_MSGTL( ( "internal:ipIfStatsTable:ipIfStatsTable_release_rowreq_ctx", "called\n" ) );

    Assert_assert( NULL != rowreq_ctx );

    ipIfStatsTable_rowreq_ctx_cleanup( rowreq_ctx );

    /*
     * for non-transient data, don't free data we got from the user
     */
    if ( ( rowreq_ctx->data ) && !( rowreq_ctx->rowreq_flags & MFD_ROW_DATA_FROM_USER ) )
        ipIfStatsTable_release_data( rowreq_ctx->data );

    /*
     * free index oid pointer
     */
    if ( rowreq_ctx->oid_idx.oids != rowreq_ctx->oid_tmp )
        free( rowreq_ctx->oid_idx.oids );

    MEMORY_FREE( rowreq_ctx );
} /* ipIfStatsTable_release_rowreq_ctx */

/**
 * @internal
 * wrapper
 */
static int
_mfd_ipIfStatsTable_pre_request( MibHandler* handler,
    HandlerRegistration* reginfo,
    AgentRequestInfo* agtreq_info,
    RequestInfo* requests )
{
    int rc;

    DEBUG_MSGTL( ( "internal:ipIfStatsTable:_mfd_ipIfStatsTable_pre_request",
        "called\n" ) );

    if ( 1 != RowMerge_statusFirst( reginfo, agtreq_info ) ) {
        DEBUG_MSGTL( ( "internal:ipIfStatsTable",
            "skipping additional pre_request\n" ) );
        return PRIOT_ERR_NOERROR;
    }

    rc = ipIfStatsTable_pre_request( ipIfStatsTable_if_ctx.user_ctx );
    if ( MFD_SUCCESS != rc ) {
        /*
         * nothing we can do about it but log it
         */
        DEBUG_MSGTL( ( "ipIfStatsTable", "error %d from "
                                         "ipIfStatsTable_pre_request\n",
            rc ) );
        Agent_requestSetErrorAll( requests, PRIOT_VALIDATE_ERR( rc ) );
    }

    return PRIOT_ERR_NOERROR;
} /* _mfd_ipIfStatsTable_pre_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_ipIfStatsTable_post_request( MibHandler* handler,
    HandlerRegistration* reginfo,
    AgentRequestInfo* agtreq_info,
    RequestInfo* requests )
{
    ipIfStatsTable_rowreq_ctx* rowreq_ctx = ( ipIfStatsTable_rowreq_ctx* )
        TableContainer_rowExtract( requests );
    int rc, packet_rc;

    DEBUG_MSGTL( ( "internal:ipIfStatsTable:_mfd_ipIfStatsTable_post_request",
        "called\n" ) );

    /*
     * release row context, if deleted
     */
    if ( rowreq_ctx && ( rowreq_ctx->rowreq_flags & MFD_ROW_DELETED ) )
        ipIfStatsTable_release_rowreq_ctx( rowreq_ctx );

    /*
     * wait for last call before calling user
     */
    if ( 1 != RowMerge_statusLast( reginfo, agtreq_info ) ) {
        DEBUG_MSGTL( ( "internal:ipIfStatsTable",
            "waiting for last post_request\n" ) );
        return PRIOT_ERR_NOERROR;
    }

    packet_rc = Agent_checkAllRequestsError( agtreq_info->asp, 0 );
    rc = ipIfStatsTable_post_request( ipIfStatsTable_if_ctx.user_ctx,
        packet_rc );
    if ( MFD_SUCCESS != rc ) {
        /*
         * nothing we can do about it but log it
         */
        DEBUG_MSGTL( ( "ipIfStatsTable", "error %d from "
                                         "ipIfStatsTable_post_request\n",
            rc ) );
    }

    return PRIOT_ERR_NOERROR;
} /* _mfd_ipIfStatsTable_post_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_ipIfStatsTable_object_lookup( MibHandler* handler,
    HandlerRegistration* reginfo,
    AgentRequestInfo* agtreq_info,
    RequestInfo* requests )
{
    int rc = PRIOT_ERR_NOERROR;
    ipIfStatsTable_rowreq_ctx* rowreq_ctx = ( ipIfStatsTable_rowreq_ctx* )
        TableContainer_rowExtract( requests );

    DEBUG_MSGTL( ( "internal:ipIfStatsTable:_mfd_ipIfStatsTable_object_lookup", "called\n" ) );

    /*
     * get our context from mfd
     * ipIfStatsTable_interface_ctx *if_ctx =
     *             (ipIfStatsTable_interface_ctx *)reginfo->my_reg_void;
     */

    if ( NULL == rowreq_ctx ) {
        rc = PRIOT_ERR_NOCREATION;
    }

    if ( MFD_SUCCESS != rc )
        Agent_requestSetErrorAll( requests, rc );
    else
        ipIfStatsTable_row_prep( rowreq_ctx );

    return PRIOT_VALIDATE_ERR( rc );
} /* _mfd_ipIfStatsTable_object_lookup */

/***********************************************************************
 *
 * GET processing
 *
 ***********************************************************************/
/*
 * @internal
 * Retrieve the value for a particular column
 */
static inline int
_ipIfStatsTable_get_column( ipIfStatsTable_rowreq_ctx* rowreq_ctx,
    VariableList* var, int column )
{
    int rc = ErrorCode_SUCCESS;

    DEBUG_MSGTL( ( "internal:ipIfStatsTable:_mfd_ipIfStatsTable_get_column",
        "called for %d\n", column ) );

    Assert_assert( NULL != rowreq_ctx );

    switch ( column ) {

    /*
         * ipIfStatsInReceives(3)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINRECEIVES:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInReceives_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCInReceives(4)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCINRECEIVES:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCInReceives_get( rowreq_ctx,
            ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsInOctets(5)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINOCTETS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInOctets_get( rowreq_ctx, ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCInOctets(6)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCINOCTETS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCInOctets_get( rowreq_ctx, ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsInHdrErrors(7)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINHDRERRORS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInHdrErrors_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsInNoRoutes(8)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINNOROUTES:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInNoRoutes_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsInAddrErrors(9)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINADDRERRORS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInAddrErrors_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsInUnknownProtos(10)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINUNKNOWNPROTOS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInUnknownProtos_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsInTruncatedPkts(11)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINTRUNCATEDPKTS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInTruncatedPkts_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsInForwDatagrams(12)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINFORWDATAGRAMS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInForwDatagrams_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCInForwDatagrams(13)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCINFORWDATAGRAMS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCInForwDatagrams_get( rowreq_ctx,
            ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsReasmReqds(14)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSREASMREQDS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsReasmReqds_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsReasmOKs(15)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSREASMOKS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsReasmOKs_get( rowreq_ctx, ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsReasmFails(16)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSREASMFAILS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsReasmFails_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsInDiscards(17)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINDISCARDS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInDiscards_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsInDelivers(18)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINDELIVERS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInDelivers_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCInDelivers(19)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCINDELIVERS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCInDelivers_get( rowreq_ctx,
            ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsOutRequests(20)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSOUTREQUESTS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsOutRequests_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCOutRequests(21)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCOUTREQUESTS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCOutRequests_get( rowreq_ctx,
            ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsOutForwDatagrams(23)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSOUTFORWDATAGRAMS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsOutForwDatagrams_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCOutForwDatagrams(24)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCOUTFORWDATAGRAMS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCOutForwDatagrams_get( rowreq_ctx,
            ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsOutDiscards(25)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSOUTDISCARDS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsOutDiscards_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsOutFragReqds(26)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSOUTFRAGREQDS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsOutFragReqds_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsOutFragOKs(27)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSOUTFRAGOKS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsOutFragOKs_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsOutFragFails(28)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSOUTFRAGFAILS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsOutFragFails_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsOutFragCreates(29)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSOUTFRAGCREATES:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsOutFragCreates_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsOutTransmits(30)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSOUTTRANSMITS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsOutTransmits_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCOutTransmits(31)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCOUTTRANSMITS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCOutTransmits_get( rowreq_ctx,
            ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsOutOctets(32)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSOUTOCTETS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsOutOctets_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCOutOctets(33)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCOUTOCTETS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCOutOctets_get( rowreq_ctx, ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsInMcastPkts(34)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINMCASTPKTS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInMcastPkts_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCInMcastPkts(35)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCINMCASTPKTS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCInMcastPkts_get( rowreq_ctx,
            ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsInMcastOctets(36)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINMCASTOCTETS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInMcastOctets_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCInMcastOctets(37)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCINMCASTOCTETS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCInMcastOctets_get( rowreq_ctx,
            ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsOutMcastPkts(38)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSOUTMCASTPKTS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsOutMcastPkts_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCOutMcastPkts(39)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCOUTMCASTPKTS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCOutMcastPkts_get( rowreq_ctx,
            ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsOutMcastOctets(40)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSOUTMCASTOCTETS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsOutMcastOctets_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCOutMcastOctets(41)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCOUTMCASTOCTETS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCOutMcastOctets_get( rowreq_ctx,
            ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsInBcastPkts(42)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSINBCASTPKTS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsInBcastPkts_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCInBcastPkts(43)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCINBCASTPKTS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCInBcastPkts_get( rowreq_ctx,
            ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsOutBcastPkts(44)/COUNTER/ASN_COUNTER/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSOUTBCASTPKTS:
        var->valueLength = sizeof( u_long );
        var->type = asnCOUNTER;
        rc = ipIfStatsOutBcastPkts_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsHCOutBcastPkts(45)/COUNTER64/ASN_COUNTER64/Int64_U64(Int64_U64)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSHCOUTBCASTPKTS:
        var->valueLength = sizeof( Integer64 );
        var->type = asnCOUNTER64;
        rc = ipIfStatsHCOutBcastPkts_get( rowreq_ctx,
            ( Integer64* )var->value.string );
        break;

    /*
         * ipIfStatsDiscontinuityTime(46)/TimeStamp/ASN_TIMETICKS/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSDISCONTINUITYTIME:
        var->valueLength = sizeof( u_long );
        var->type = asnTIMETICKS;
        rc = ipIfStatsDiscontinuityTime_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipIfStatsRefreshRate(47)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/A/w/e/r/d/h 
         */
    case COLUMN_IPIFSTATSREFRESHRATE:
        var->valueLength = sizeof( u_long );
        var->type = asnUNSIGNED;
        rc = ipIfStatsRefreshRate_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    default:
        if ( IPIFSTATSTABLE_MIN_COL <= column
            && column <= IPIFSTATSTABLE_MAX_COL ) {
            DEBUG_MSGTL( ( "internal:ipIfStatsTable:_mfd_ipIfStatsTable_get_column", "assume column %d is reserved\n", column ) );
            rc = MFD_SKIP;
        } else {
            Logger_log( LOGGER_PRIORITY_ERR,
                "unknown column %d in _ipIfStatsTable_get_column\n",
                column );
        }
        break;
    }

    return rc;
} /* _ipIfStatsTable_get_column */

int _mfd_ipIfStatsTable_get_values( MibHandler* handler,
    HandlerRegistration* reginfo,
    AgentRequestInfo* agtreq_info,
    RequestInfo* requests )
{
    ipIfStatsTable_rowreq_ctx* rowreq_ctx = ( ipIfStatsTable_rowreq_ctx* )
        TableContainer_rowExtract( requests );
    TableRequestInfo* tri;
    u_char* old_string;
    void ( *dataFreeHook )( void* );
    int rc;

    DEBUG_MSGTL( ( "internal:ipIfStatsTable:_mfd_ipIfStatsTable_get_values",
        "called\n" ) );

    Assert_assert( NULL != rowreq_ctx );

    for ( ; requests; requests = requests->next ) {
        /*
         * save old pointer, so we can free it if replaced
         */
        old_string = requests->requestvb->value.string;
        dataFreeHook = requests->requestvb->dataFreeHook;
        if ( NULL == requests->requestvb->value.string ) {
            requests->requestvb->value.string = requests->requestvb->buffer;
            requests->requestvb->valueLength = sizeof( requests->requestvb->buffer );
        } else if ( requests->requestvb->buffer == requests->requestvb->value.string ) {
            if ( requests->requestvb->valueLength != sizeof( requests->requestvb->buffer ) )
                requests->requestvb->valueLength = sizeof( requests->requestvb->buffer );
        }

        /*
         * get column data
         */
        tri = Table_extractTableInfo( requests );
        if ( NULL == tri )
            continue;

        rc = _ipIfStatsTable_get_column( rowreq_ctx, requests->requestvb,
            tri->colnum );
        if ( rc ) {
            if ( MFD_SKIP == rc ) {
                requests->requestvb->type = PRIOT_NOSUCHINSTANCE;
                rc = PRIOT_ERR_NOERROR;
            }
        } else if ( NULL == requests->requestvb->value.string ) {
            Logger_log( LOGGER_PRIORITY_ERR, "NULL varbind data pointer!\n" );
            rc = PRIOT_ERR_GENERR;
        }
        if ( rc )
            Agent_requestSetError( requests, PRIOT_VALIDATE_ERR( rc ) );

        /*
         * if the buffer wasn't used previously for the old data (i.e. it
         * was allcoated memory)  and the get routine replaced the pointer,
         * we need to free the previous pointer.
         */
        if ( old_string && ( old_string != requests->requestvb->buffer ) && ( requests->requestvb->value.string != old_string ) ) {
            if ( dataFreeHook )
                ( *dataFreeHook )( old_string );
            else
                free( old_string );
        }
    } /* for results */

    return PRIOT_ERR_NOERROR;
} /* _mfd_ipIfStatsTable_get_values */

/***********************************************************************
 *
 * SET processing
 *
 ***********************************************************************/

/*
 * SET PROCESSING NOT APPLICABLE (per MIB or user setting)
 */
/***********************************************************************
 *
 * DATA ACCESS
 *
 ***********************************************************************/
static void _container_free( Container_Container* container );

/**
 * @internal
 */
static int
_cache_load( Cache* cache, void* vmagic )
{
    DEBUG_MSGTL( ( "internal:ipIfStatsTable:_cache_load", "called\n" ) );

    if ( ( NULL == cache ) || ( NULL == cache->magic ) ) {
        Logger_log( LOGGER_PRIORITY_ERR, "invalid cache for ipIfStatsTable_cache_load\n" );
        return -1;
    }

    /** should only be called for an invalid or expired cache */
    Assert_assert( ( 0 == cache->valid ) || ( 1 == cache->expired ) );

    /*
     * call user code
     */
    return ipIfStatsTable_container_load( ( Container_Container* )cache->magic );
} /* _cache_load */

/**
 * @internal
 */
static void
_cache_free( Cache* cache, void* magic )
{
    Container_Container* container;

    DEBUG_MSGTL( ( "internal:ipIfStatsTable:_cache_free", "called\n" ) );

    if ( ( NULL == cache ) || ( NULL == cache->magic ) ) {
        Logger_log( LOGGER_PRIORITY_ERR, "invalid cache in ipIfStatsTable_cache_free\n" );
        return;
    }

    container = ( Container_Container* )cache->magic;

    _container_free( container );
} /* _cache_free */

/**
 * @internal
 */
static void
_container_item_free( ipIfStatsTable_rowreq_ctx* rowreq_ctx, void* context )
{
    DEBUG_MSGTL( ( "internal:ipIfStatsTable:_container_item_free",
        "called\n" ) );

    if ( NULL == rowreq_ctx )
        return;

    ipIfStatsTable_release_rowreq_ctx( rowreq_ctx );
} /* _container_item_free */

/**
 * @internal
 */
static void
_container_free( Container_Container* container )
{
    DEBUG_MSGTL( ( "internal:ipIfStatsTable:_container_free", "called\n" ) );

    if ( NULL == container ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "invalid container in ipIfStatsTable_container_free\n" );
        return;
    }

    /*
     * call user code
     */
    ipIfStatsTable_container_free( container );

    /*
     * free all items. inefficient, but easy.
     */
    CONTAINER_CLEAR( container,
        ( Container_FuncObjFunc* )_container_item_free,
        NULL );
} /* _container_free */

/**
 * @internal
 * initialize the container with functions or wrappers
 */
void _ipIfStatsTable_container_init( ipIfStatsTable_interface_ctx* if_ctx )
{
    DEBUG_MSGTL( ( "internal:ipIfStatsTable:_ipIfStatsTable_container_init",
        "called\n" ) );

    /*
     * cache init
     */
    if_ctx->cache = CacheHandler_create( 30, /* timeout in seconds */
        _cache_load, _cache_free,
        ipIfStatsTable_oid,
        ipIfStatsTable_oid_size );

    if ( NULL == if_ctx->cache ) {
        Logger_log( LOGGER_PRIORITY_ERR, "error creating cache for ipIfStatsTable\n" );
        return;
    }

    if_ctx->cache->flags = CacheOperation_DONT_INVALIDATE_ON_SET;

    ipIfStatsTable_container_init( &if_ctx->container, if_ctx->cache );
    if ( NULL == if_ctx->container )
        if_ctx->container = Container_find( "ipIfStatsTable:tableContainer" );
    if ( NULL == if_ctx->container ) {
        Logger_log( LOGGER_PRIORITY_ERR, "error creating container in "
                                         "ipIfStatsTable_container_init\n" );
        return;
    }

    if ( NULL != if_ctx->cache )
        if_ctx->cache->magic = ( void* )if_ctx->container;
} /* _ipIfStatsTable_container_init */

/**
 * @internal
 * shutdown the container with functions or wrappers
 */
void _ipIfStatsTable_container_shutdown( ipIfStatsTable_interface_ctx* if_ctx )
{
    DEBUG_MSGTL( ( "internal:ipIfStatsTable:_ipIfStatsTable_container_shutdown", "called\n" ) );

    ipIfStatsTable_container_shutdown( if_ctx->container );

    _container_free( if_ctx->container );

} /* _ipIfStatsTable_container_shutdown */

ipIfStatsTable_rowreq_ctx*
ipIfStatsTable_row_find_by_mib_index( ipIfStatsTable_mib_index* mib_idx )
{
    ipIfStatsTable_rowreq_ctx* rowreq_ctx;
    oid oid_tmp[ asnMAX_OID_LEN ];
    Types_Index oid_idx;
    int rc;

    /*
     * set up storage for OID
     */
    oid_idx.oids = oid_tmp;
    oid_idx.len = sizeof( oid_tmp ) / sizeof( oid );

    /*
     * convert
     */
    rc = ipIfStatsTable_index_to_oid( &oid_idx, mib_idx );
    if ( MFD_SUCCESS != rc )
        return NULL;

    rowreq_ctx = ( ipIfStatsTable_rowreq_ctx* )CONTAINER_FIND( ipIfStatsTable_if_ctx.container, &oid_idx );

    return rowreq_ctx;
}
