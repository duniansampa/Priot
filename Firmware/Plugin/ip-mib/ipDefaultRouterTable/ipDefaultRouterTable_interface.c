/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 15899 $ of $ 
 *
 * $Id:$
 */
/*
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 * ***                                                               ***
 * ***  NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE  ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THIS FILE DOES NOT CONTAIN ANY USER EDITABLE CODE.      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THE GENERATED CODE IS INTERNAL IMPLEMENTATION, AND      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***    IS SUBJECT TO CHANGE WITHOUT WARNING IN FUTURE RELEASES.   ***
 * ***                                                               ***
 * ***                                                               ***
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 */

/*
 * include our parent header 
 */
#include "ipDefaultRouterTable.h"
#include "System/Util/Assert.h"
#include "BabySteps.h"
#include "CacheHandler.h"
#include "Client.h"
#include "System/Util/Trace.h"
#include "System/Util/Logger.h"
#include "Mib.h"
#include "RowMerge.h"
#include "TableContainer.h"
#include "ipDefaultRouterTable_data_access.h"
#include "ipDefaultRouterTable_data_get.h"
#include "ipDefaultRouterTable_interface.h"
#include "ipDefaultRouterTable_oids.h"
#include "siglog/agent/mfd.h"

/**********************************************************************
 **********************************************************************
 ***
 *** Table ipDefaultRouterTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * IP-MIB::ipDefaultRouterTable is subid 37 of ip.
 * Its status is Current.
 * OID: .1.3.6.1.2.1.4.37, length: 8
 */
typedef struct ipDefaultRouterTable_interface_ctx_s {

    Container_Container* container;
    Cache* cache;

    ipDefaultRouterTable_registration* user_ctx;

    TableRegistrationInfo tbl_info;

    BabyStepsAccessMethods access_multiplexer;

} ipDefaultRouterTable_interface_ctx;

static ipDefaultRouterTable_interface_ctx ipDefaultRouterTable_if_ctx;

static void
_ipDefaultRouterTable_container_init( ipDefaultRouterTable_interface_ctx*
        if_ctx );
static void
_ipDefaultRouterTable_container_shutdown( ipDefaultRouterTable_interface_ctx* if_ctx );

Container_Container*
ipDefaultRouterTable_container_get( void )
{
    return ipDefaultRouterTable_if_ctx.container;
}

ipDefaultRouterTable_registration*
ipDefaultRouterTable_registration_get( void )
{
    return ipDefaultRouterTable_if_ctx.user_ctx;
}

ipDefaultRouterTable_registration*
ipDefaultRouterTable_registration_set( ipDefaultRouterTable_registration*
        newreg )
{
    ipDefaultRouterTable_registration* old = ipDefaultRouterTable_if_ctx.user_ctx;
    ipDefaultRouterTable_if_ctx.user_ctx = newreg;
    return old;
}

int ipDefaultRouterTable_container_size( void )
{
    return CONTAINER_SIZE( ipDefaultRouterTable_if_ctx.container );
}

/*
 * mfd multiplexer modes
 */
static NodeHandlerFT _mfd_ipDefaultRouterTable_pre_request;
static NodeHandlerFT _mfd_ipDefaultRouterTable_post_request;
static NodeHandlerFT _mfd_ipDefaultRouterTable_object_lookup;
static NodeHandlerFT _mfd_ipDefaultRouterTable_get_values;
/**
 * @internal
 * Initialize the table ipDefaultRouterTable 
 *    (Define its contents and how it's structured)
 */
void _ipDefaultRouterTable_initialize_interface( ipDefaultRouterTable_registration* reg_ptr, u_long flags )
{
    BabyStepsAccessMethods* access_multiplexer = &ipDefaultRouterTable_if_ctx.access_multiplexer;
    TableRegistrationInfo* tbl_info = &ipDefaultRouterTable_if_ctx.tbl_info;
    HandlerRegistration* reginfo;
    MibHandler* handler;
    int mfd_modes = 0;

    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_ipDefaultRouterTable_initialize_interface", "called\n" ) );

    /*************************************************
     *
     * save interface context for ipDefaultRouterTable
     */
    /*
     * Setting up the table's definition
     */
    Table_helperAddIndexes( tbl_info, asnINTEGER,
        /** index: ipDefaultRouterAddressType */
        asnOCTET_STR,
        /** index: ipDefaultRouterAddress */
        asnINTEGER,
        /** index: ipDefaultRouterIfIndex */
        0 );

    /*
     * Define the minimum and maximum accessible columns.  This
     * optimizes retrieval. 
     */
    tbl_info->min_column = IPDEFAULTROUTERTABLE_MIN_COL;
    tbl_info->max_column = IPDEFAULTROUTERTABLE_MAX_COL;

    /*
     * save users context
     */
    ipDefaultRouterTable_if_ctx.user_ctx = reg_ptr;

    /*
     * call data access initialization code
     */
    ipDefaultRouterTable_init_data( reg_ptr );

    /*
     * set up the container
     */
    _ipDefaultRouterTable_container_init( &ipDefaultRouterTable_if_ctx );
    if ( NULL == ipDefaultRouterTable_if_ctx.container ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "could not initialize container for ipDefaultRouterTable\n" );
        return;
    }

    /*
     * access_multiplexer: REQUIRED wrapper for get request handling
     */
    access_multiplexer->object_lookup = _mfd_ipDefaultRouterTable_object_lookup;
    access_multiplexer->get_values = _mfd_ipDefaultRouterTable_get_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->pre_request = _mfd_ipDefaultRouterTable_pre_request;
    access_multiplexer->post_request = _mfd_ipDefaultRouterTable_post_request;

    /*************************************************
     *
     * Create a registration, save our reg data, register table.
     */
    DEBUG_MSGTL( ( "ipDefaultRouterTable:init_ipDefaultRouterTable",
        "Registering ipDefaultRouterTable as a mibs-for-dummies table.\n" ) );
    handler = BabySteps_accessMultiplexerGet( access_multiplexer );
    reginfo = AgentHandler_registrationCreate( "ipDefaultRouterTable",
        handler,
        ipDefaultRouterTable_oid,
        ipDefaultRouterTable_oid_size,
        HANDLER_CAN_BABY_STEP | HANDLER_CAN_RONLY );
    if ( NULL == reginfo ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "error registering table ipDefaultRouterTable\n" );
        return;
    }
    reginfo->my_reg_void = &ipDefaultRouterTable_if_ctx;

    /*************************************************
     *
     * set up baby steps handler, create it and inject it
     */
    if ( access_multiplexer->object_lookup )
        mfd_modes |= BabyStepsMode_OBJECT_LOOKUP;
    if ( access_multiplexer->pre_request )
        mfd_modes |= BabyStepsMode_PRE_REQUEST;
    if ( access_multiplexer->post_request )
        mfd_modes |= BabyStepsMode_POST_REQUEST;

    /* XXX - are these actually necessary? */
    if ( access_multiplexer->set_values )
        mfd_modes |= BabyStepsMode_SET_VALUES;
    if ( access_multiplexer->irreversible_commit )
        mfd_modes |= BabyStepsMode_IRREVERSIBLE_COMMIT;
    if ( access_multiplexer->object_syntax_checks )
        mfd_modes |= BabyStepsMode_CHECK_OBJECT;

    if ( access_multiplexer->undo_setup )
        mfd_modes |= BabyStepsMode_UNDO_SETUP;
    if ( access_multiplexer->undo_cleanup )
        mfd_modes |= BabyStepsMode_UNDO_CLEANUP;
    if ( access_multiplexer->undo_sets )
        mfd_modes |= BabyStepsMode_UNDO_SETS;

    if ( access_multiplexer->row_creation )
        mfd_modes |= BabyStepsMode_ROW_CREATE;
    if ( access_multiplexer->consistency_checks )
        mfd_modes |= BabyStepsMode_CHECK_CONSISTENCY;
    if ( access_multiplexer->commit )
        mfd_modes |= BabyStepsMode_COMMIT;
    if ( access_multiplexer->undo_commit )
        mfd_modes |= BabyStepsMode_UNDO_COMMIT;

    handler = BabySteps_handlerGet( mfd_modes );
    AgentHandler_injectHandler( reginfo, handler );

    /*************************************************
     *
     * inject row_merge helper with prefix rootoid_len + 2 (entry.col)
     */
    handler = RowMerge_getRowMergeHandler( reginfo->rootoid_len + 2 );
    AgentHandler_injectHandler( reginfo, handler );

    /*************************************************
     *
     * inject container_table helper
     */
    handler = TableContainer_handlerGet( tbl_info,
        ipDefaultRouterTable_if_ctx.container,
        TABLE_CONTAINER_KEY_NETSNMP_INDEX );
    AgentHandler_injectHandler( reginfo, handler );

    /*************************************************
     *
     * inject cache helper
     */
    if ( NULL != ipDefaultRouterTable_if_ctx.cache ) {
        handler = CacheHandler_handlerGet( ipDefaultRouterTable_if_ctx.cache );
        AgentHandler_injectHandler( reginfo, handler );
    }

    /*
     * register table
     */
    Table_registerTable( reginfo, tbl_info );

} /* _ipDefaultRouterTable_initialize_interface */

/**
 * @internal
 * Shutdown the table ipDefaultRouterTable
 */
void _ipDefaultRouterTable_shutdown_interface( ipDefaultRouterTable_registration* reg_ptr )
{
    /*
     * shutdown the container
     */
    _ipDefaultRouterTable_container_shutdown( &ipDefaultRouterTable_if_ctx );
}

void ipDefaultRouterTable_valid_columns_set( ColumnInfo* vc )
{
    ipDefaultRouterTable_if_ctx.tbl_info.valid_columns = vc;
} /* ipDefaultRouterTable_valid_columns_set */

/**
 * @internal
 * convert the index component stored in the context to an oid
 */
int ipDefaultRouterTable_index_to_oid( Types_Index* oid_idx,
    ipDefaultRouterTable_mib_index* mib_idx )
{
    int err = PRIOT_ERR_NOERROR;

    /*
     * temp storage for parsing indexes
     */
    /*
     * ipDefaultRouterAddressType(1)/InetAddressType/ASN_INTEGER/long(u_long)//l/a/w/E/r/d/h
     */
    VariableList var_ipDefaultRouterAddressType;
    /*
     * ipDefaultRouterAddress(2)/InetAddress/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h
     */
    VariableList var_ipDefaultRouterAddress;
    /*
     * ipDefaultRouterIfIndex(3)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H
     */
    VariableList var_ipDefaultRouterIfIndex;

    /*
     * set up varbinds
     */
    memset( &var_ipDefaultRouterAddressType, 0x00,
        sizeof( var_ipDefaultRouterAddressType ) );
    var_ipDefaultRouterAddressType.type = asnINTEGER;
    memset( &var_ipDefaultRouterAddress, 0x00,
        sizeof( var_ipDefaultRouterAddress ) );
    var_ipDefaultRouterAddress.type = asnOCTET_STR;
    memset( &var_ipDefaultRouterIfIndex, 0x00,
        sizeof( var_ipDefaultRouterIfIndex ) );
    var_ipDefaultRouterIfIndex.type = asnINTEGER;

    /*
     * chain temp index varbinds together
     */
    var_ipDefaultRouterAddressType.next = &var_ipDefaultRouterAddress;
    var_ipDefaultRouterAddress.next = &var_ipDefaultRouterIfIndex;
    var_ipDefaultRouterIfIndex.next = NULL;

    DEBUG_MSGTL( ( "verbose:ipDefaultRouterTable:ipDefaultRouterTable_index_to_oid", "called\n" ) );

    /*
     * ipDefaultRouterAddressType(1)/InetAddressType/ASN_INTEGER/long(u_long)//l/a/w/E/r/d/h 
     */
    Client_setVarValue( &var_ipDefaultRouterAddressType,
        ( u_char* )&mib_idx->ipDefaultRouterAddressType,
        sizeof( mib_idx->ipDefaultRouterAddressType ) );

    /*
     * ipDefaultRouterAddress(2)/InetAddress/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h 
     */
    Client_setVarValue( &var_ipDefaultRouterAddress,
        ( u_char* )&mib_idx->ipDefaultRouterAddress,
        mib_idx->ipDefaultRouterAddress_len * sizeof( mib_idx->ipDefaultRouterAddress[ 0 ] ) );

    /*
     * ipDefaultRouterIfIndex(3)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H 
     */
    Client_setVarValue( &var_ipDefaultRouterIfIndex,
        ( u_char* )&mib_idx->ipDefaultRouterIfIndex,
        sizeof( mib_idx->ipDefaultRouterIfIndex ) );

    err = Mib_buildOidNoalloc( oid_idx->oids, oid_idx->len, &oid_idx->len,
        NULL, 0, &var_ipDefaultRouterAddressType );
    if ( err )
        Logger_log( LOGGER_PRIORITY_ERR, "error %d converting index to oid\n", err );

    /*
     * parsing may have allocated memory. free it.
     */
    Client_resetVarBuffers( &var_ipDefaultRouterAddressType );

    return err;
} /* ipDefaultRouterTable_index_to_oid */

/**
 * extract ipDefaultRouterTable indexes from a Types_Index
 *
 * @retval PRIOT_ERR_NOERROR  : no error
 * @retval PRIOT_ERR_GENERR   : error
 */
int ipDefaultRouterTable_index_from_oid( Types_Index* oid_idx,
    ipDefaultRouterTable_mib_index*
        mib_idx )
{
    int err = PRIOT_ERR_NOERROR;

    /*
     * temp storage for parsing indexes
     */
    /*
     * ipDefaultRouterAddressType(1)/InetAddressType/ASN_INTEGER/long(u_long)//l/a/w/E/r/d/h
     */
    VariableList var_ipDefaultRouterAddressType;
    /*
     * ipDefaultRouterAddress(2)/InetAddress/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h
     */
    VariableList var_ipDefaultRouterAddress;
    /*
     * ipDefaultRouterIfIndex(3)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H
     */
    VariableList var_ipDefaultRouterIfIndex;

    /*
     * set up varbinds
     */
    memset( &var_ipDefaultRouterAddressType, 0x00,
        sizeof( var_ipDefaultRouterAddressType ) );
    var_ipDefaultRouterAddressType.type = asnINTEGER;
    memset( &var_ipDefaultRouterAddress, 0x00,
        sizeof( var_ipDefaultRouterAddress ) );
    var_ipDefaultRouterAddress.type = asnOCTET_STR;
    memset( &var_ipDefaultRouterIfIndex, 0x00,
        sizeof( var_ipDefaultRouterIfIndex ) );
    var_ipDefaultRouterIfIndex.type = asnINTEGER;

    /*
     * chain temp index varbinds together
     */
    var_ipDefaultRouterAddressType.next = &var_ipDefaultRouterAddress;
    var_ipDefaultRouterAddress.next = &var_ipDefaultRouterIfIndex;
    var_ipDefaultRouterIfIndex.next = NULL;

    DEBUG_MSGTL( ( "verbose:ipDefaultRouterTable:ipDefaultRouterTable_index_from_oid", "called\n" ) );

    /*
     * parse the oid into the individual index components
     */
    err = Mib_parseOidIndexes( oid_idx->oids, oid_idx->len,
        &var_ipDefaultRouterAddressType );
    if ( err == PRIOT_ERR_NOERROR ) {
        /*
         * copy out values
         */
        mib_idx->ipDefaultRouterAddressType = *( ( u_long* )var_ipDefaultRouterAddressType.value.string );
        /*
         * NOTE: val_len is in bytes, ipDefaultRouterAddress_len might not be
         */
        if ( var_ipDefaultRouterAddress.valueLength > sizeof( mib_idx->ipDefaultRouterAddress ) )
            err = PRIOT_ERR_GENERR;
        else {
            memcpy( mib_idx->ipDefaultRouterAddress,
                var_ipDefaultRouterAddress.value.string,
                var_ipDefaultRouterAddress.valueLength );
            mib_idx->ipDefaultRouterAddress_len = var_ipDefaultRouterAddress.valueLength / sizeof( mib_idx->ipDefaultRouterAddress[ 0 ] );
        }
        mib_idx->ipDefaultRouterIfIndex = *( ( long* )var_ipDefaultRouterIfIndex.value.string );
    }

    /*
     * parsing may have allocated memory. free it.
     */
    Client_resetVarBuffers( &var_ipDefaultRouterAddressType );

    return err;
} /* ipDefaultRouterTable_index_from_oid */

/*
 *********************************************************************
 * @internal
 * allocate resources for a ipDefaultRouterTable_rowreq_ctx
 */
ipDefaultRouterTable_rowreq_ctx*
ipDefaultRouterTable_allocate_rowreq_ctx( ipDefaultRouterTable_data* data,
    void* user_init_ctx )
{
    ipDefaultRouterTable_rowreq_ctx* rowreq_ctx = MEMORY_MALLOC_TYPEDEF( ipDefaultRouterTable_rowreq_ctx );

    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:ipDefaultRouterTable_allocate_rowreq_ctx", "called\n" ) );

    if ( NULL == rowreq_ctx ) {
        Logger_log( LOGGER_PRIORITY_ERR, "Couldn't allocate memory for a "
                                         "ipDefaultRouterTable_rowreq_ctx.\n" );
        return NULL;
    } else {
        if ( NULL != data ) {
            /*
             * track if we got data from user
             */
            rowreq_ctx->rowreq_flags |= MFD_ROW_DATA_FROM_USER;
            rowreq_ctx->data = data;
        } else if ( NULL == ( rowreq_ctx->data = ipDefaultRouterTable_allocate_data() ) ) {
            MEMORY_FREE( rowreq_ctx );
            return NULL;
        }
    }

    /*
     * undo context will be allocated when needed (in *_undo_setup)
     */

    rowreq_ctx->oid_idx.oids = rowreq_ctx->oid_tmp;

    rowreq_ctx->ipDefaultRouterTable_data_list = NULL;

    /*
     * if we allocated data, call init routine
     */
    if ( !( rowreq_ctx->rowreq_flags & MFD_ROW_DATA_FROM_USER ) ) {
        if ( ErrorCode_SUCCESS != ipDefaultRouterTable_rowreq_ctx_init( rowreq_ctx,
                                      user_init_ctx ) ) {
            ipDefaultRouterTable_release_rowreq_ctx( rowreq_ctx );
            rowreq_ctx = NULL;
        }
    }

    return rowreq_ctx;
} /* ipDefaultRouterTable_allocate_rowreq_ctx */

/*
 * @internal
 * release resources for a ipDefaultRouterTable_rowreq_ctx
 */
void ipDefaultRouterTable_release_rowreq_ctx( ipDefaultRouterTable_rowreq_ctx*
        rowreq_ctx )
{
    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:ipDefaultRouterTable_release_rowreq_ctx", "called\n" ) );

    Assert_assert( NULL != rowreq_ctx );

    ipDefaultRouterTable_rowreq_ctx_cleanup( rowreq_ctx );

    /*
     * for non-transient data, don't free data we got from the user
     */
    if ( ( rowreq_ctx->data ) && !( rowreq_ctx->rowreq_flags & MFD_ROW_DATA_FROM_USER ) )
        ipDefaultRouterTable_release_data( rowreq_ctx->data );

    /*
     * free index oid pointer
     */
    if ( rowreq_ctx->oid_idx.oids != rowreq_ctx->oid_tmp )
        free( rowreq_ctx->oid_idx.oids );

    MEMORY_FREE( rowreq_ctx );
} /* ipDefaultRouterTable_release_rowreq_ctx */

/**
 * @internal
 * wrapper
 */
static int
_mfd_ipDefaultRouterTable_pre_request( MibHandler* handler,
    HandlerRegistration* reginfo,
    AgentRequestInfo* agtreq_info,
    RequestInfo* requests )
{
    int rc;

    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_mfd_ipDefaultRouterTable_pre_request", "called\n" ) );

    if ( 1 != RowMerge_statusFirst( reginfo, agtreq_info ) ) {
        DEBUG_MSGTL( ( "internal:ipDefaultRouterTable",
            "skipping additional pre_request\n" ) );
        return PRIOT_ERR_NOERROR;
    }

    rc = ipDefaultRouterTable_pre_request( ipDefaultRouterTable_if_ctx.user_ctx );
    if ( MFD_SUCCESS != rc ) {
        /*
         * nothing we can do about it but log it
         */
        DEBUG_MSGTL( ( "ipDefaultRouterTable", "error %d from "
                                               "ipDefaultRouterTable_pre_request\n",
            rc ) );
        Agent_requestSetErrorAll( requests, PRIOT_VALIDATE_ERR( rc ) );
    }

    return PRIOT_ERR_NOERROR;
} /* _mfd_ipDefaultRouterTable_pre_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_ipDefaultRouterTable_post_request( MibHandler* handler,
    HandlerRegistration* reginfo,
    AgentRequestInfo* agtreq_info,
    RequestInfo* requests )
{
    ipDefaultRouterTable_rowreq_ctx* rowreq_ctx = ( ipDefaultRouterTable_rowreq_ctx* )TableContainer_rowExtract( requests );
    int rc, packet_rc;

    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_mfd_ipDefaultRouterTable_post_request", "called\n" ) );

    /*
     * release row context, if deleted
     */
    if ( rowreq_ctx && ( rowreq_ctx->rowreq_flags & MFD_ROW_DELETED ) )
        ipDefaultRouterTable_release_rowreq_ctx( rowreq_ctx );

    /*
     * wait for last call before calling user
     */
    if ( 1 != RowMerge_statusLast( reginfo, agtreq_info ) ) {
        DEBUG_MSGTL( ( "internal:ipDefaultRouterTable",
            "waiting for last post_request\n" ) );
        return PRIOT_ERR_NOERROR;
    }

    packet_rc = Agent_checkAllRequestsError( agtreq_info->asp, 0 );
    rc = ipDefaultRouterTable_post_request( ipDefaultRouterTable_if_ctx.user_ctx, packet_rc );
    if ( MFD_SUCCESS != rc ) {
        /*
         * nothing we can do about it but log it
         */
        DEBUG_MSGTL( ( "ipDefaultRouterTable", "error %d from "
                                               "ipDefaultRouterTable_post_request\n",
            rc ) );
    }

    return PRIOT_ERR_NOERROR;
} /* _mfd_ipDefaultRouterTable_post_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_ipDefaultRouterTable_object_lookup( MibHandler* handler,
    HandlerRegistration* reginfo,
    AgentRequestInfo* agtreq_info,
    RequestInfo* requests )
{
    int rc = PRIOT_ERR_NOERROR;
    ipDefaultRouterTable_rowreq_ctx* rowreq_ctx = ( ipDefaultRouterTable_rowreq_ctx* )TableContainer_rowExtract( requests );

    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_mfd_ipDefaultRouterTable_object_lookup", "called\n" ) );

    /*
     * get our context from mfd
     * ipDefaultRouterTable_interface_ctx *if_ctx =
     *             (ipDefaultRouterTable_interface_ctx *)reginfo->my_reg_void;
     */

    if ( NULL == rowreq_ctx ) {
        rc = PRIOT_ERR_NOCREATION;
    }

    if ( MFD_SUCCESS != rc )
        Agent_requestSetErrorAll( requests, rc );
    else
        ipDefaultRouterTable_row_prep( rowreq_ctx );

    return PRIOT_VALIDATE_ERR( rc );
} /* _mfd_ipDefaultRouterTable_object_lookup */

/***********************************************************************
 *
 * GET processing
 *
 ***********************************************************************/
/*
 * @internal
 * Retrieve the value for a particular column
 */
static inline int
_ipDefaultRouterTable_get_column( ipDefaultRouterTable_rowreq_ctx*
                                      rowreq_ctx,
    VariableList* var,
    int column )
{
    int rc = ErrorCode_SUCCESS;

    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_mfd_ipDefaultRouterTable_get_column", "called for %d\n", column ) );

    Assert_assert( NULL != rowreq_ctx );

    switch ( column ) {

    /*
         * ipDefaultRouterLifetime(4)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/A/w/e/R/d/h 
         */
    case COLUMN_IPDEFAULTROUTERLIFETIME:
        var->valueLength = sizeof( u_long );
        var->type = asnUNSIGNED;
        rc = ipDefaultRouterLifetime_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    /*
         * ipDefaultRouterPreference(5)/INTEGER/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h 
         */
    case COLUMN_IPDEFAULTROUTERPREFERENCE:
        var->valueLength = sizeof( u_long );
        var->type = asnINTEGER;
        rc = ipDefaultRouterPreference_get( rowreq_ctx,
            ( u_long* )var->value.string );
        break;

    default:
        if ( IPDEFAULTROUTERTABLE_MIN_COL <= column
            && column <= IPDEFAULTROUTERTABLE_MAX_COL ) {
            DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_mfd_ipDefaultRouterTable_get_column", "assume column %d is reserved\n", column ) );
            rc = MFD_SKIP;
        } else {
            Logger_log( LOGGER_PRIORITY_ERR,
                "unknown column %d in _ipDefaultRouterTable_get_column\n",
                column );
        }
        break;
    }

    return rc;
} /* _ipDefaultRouterTable_get_column */

int _mfd_ipDefaultRouterTable_get_values( MibHandler* handler,
    HandlerRegistration* reginfo,
    AgentRequestInfo* agtreq_info,
    RequestInfo* requests )
{
    ipDefaultRouterTable_rowreq_ctx* rowreq_ctx = ( ipDefaultRouterTable_rowreq_ctx* )
        TableContainer_rowExtract( requests );
    TableRequestInfo* tri;
    u_char* old_string;
    void ( *dataFreeHook )( void* );
    int rc;

    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_mfd_ipDefaultRouterTable_get_values", "called\n" ) );

    Assert_assert( NULL != rowreq_ctx );

    for ( ; requests; requests = requests->next ) {
        /*
         * save old pointer, so we can free it if replaced
         */
        old_string = requests->requestvb->value.string;
        dataFreeHook = requests->requestvb->dataFreeHook;
        if ( NULL == requests->requestvb->value.string ) {
            requests->requestvb->value.string = requests->requestvb->buffer;
            requests->requestvb->valueLength = sizeof( requests->requestvb->buffer );
        } else if ( requests->requestvb->buffer == requests->requestvb->value.string ) {
            if ( requests->requestvb->valueLength != sizeof( requests->requestvb->buffer ) )
                requests->requestvb->valueLength = sizeof( requests->requestvb->buffer );
        }

        /*
         * get column data
         */
        tri = Table_extractTableInfo( requests );
        if ( NULL == tri )
            continue;

        rc = _ipDefaultRouterTable_get_column( rowreq_ctx,
            requests->requestvb,
            tri->colnum );
        if ( rc ) {
            if ( MFD_SKIP == rc ) {
                requests->requestvb->type = PRIOT_NOSUCHINSTANCE;
                rc = PRIOT_ERR_NOERROR;
            }
        } else if ( NULL == requests->requestvb->value.string ) {
            Logger_log( LOGGER_PRIORITY_ERR, "NULL varbind data pointer!\n" );
            rc = PRIOT_ERR_GENERR;
        }
        if ( rc )
            Agent_requestSetError( requests, PRIOT_VALIDATE_ERR( rc ) );

        /*
         * if the buffer wasn't used previously for the old data (i.e. it
         * was allcoated memory)  and the get routine replaced the pointer,
         * we need to free the previous pointer.
         */
        if ( old_string && ( old_string != requests->requestvb->buffer ) && ( requests->requestvb->value.string != old_string ) ) {
            if ( dataFreeHook )
                ( *dataFreeHook )( old_string );
            else
                free( old_string );
        }
    } /* for results */

    return PRIOT_ERR_NOERROR;
} /* _mfd_ipDefaultRouterTable_get_values */

/***********************************************************************
 *
 * SET processing
 *
 ***********************************************************************/

/*
 * SET PROCESSING NOT APPLICABLE (per MIB or user setting)
 */
/***********************************************************************
 *
 * DATA ACCESS
 *
 ***********************************************************************/
static void _container_free( Container_Container* container );

/**
 * @internal
 */
static int
_cache_load( Cache* cache, void* vmagic )
{
    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_cache_load", "called\n" ) );

    if ( ( NULL == cache ) || ( NULL == cache->magic ) ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "invalid cache for ipDefaultRouterTable_cache_load\n" );
        return -1;
    }

    /** should only be called for an invalid or expired cache */
    Assert_assert( ( 0 == cache->valid ) || ( 1 == cache->expired ) );

    /*
     * call user code
     */
    return ipDefaultRouterTable_container_load( ( Container_Container* )
                                                    cache->magic );
} /* _cache_load */

/**
 * @internal
 */
static void
_cache_free( Cache* cache, void* magic )
{
    Container_Container* container;

    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_cache_free", "called\n" ) );

    if ( ( NULL == cache ) || ( NULL == cache->magic ) ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "invalid cache in ipDefaultRouterTable_cache_free\n" );
        return;
    }

    container = ( Container_Container* )cache->magic;

    _container_free( container );
} /* _cache_free */

/**
 * @internal
 */
static void
_container_item_free( ipDefaultRouterTable_rowreq_ctx* rowreq_ctx,
    void* context )
{
    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_container_item_free",
        "called\n" ) );

    if ( NULL == rowreq_ctx )
        return;

    ipDefaultRouterTable_release_rowreq_ctx( rowreq_ctx );
} /* _container_item_free */

/**
 * @internal
 */
static void
_container_free( Container_Container* container )
{
    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_container_free",
        "called\n" ) );

    if ( NULL == container ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "invalid container in ipDefaultRouterTable_container_free\n" );
        return;
    }

    /*
     * call user code
     */
    ipDefaultRouterTable_container_free( container );

    /*
     * free all items. inefficient, but easy.
     */
    CONTAINER_CLEAR( container,
        ( Container_FuncObjFunc* )_container_item_free,
        NULL );
} /* _container_free */

/**
 * @internal
 * initialize the container with functions or wrappers
 */
void _ipDefaultRouterTable_container_init( ipDefaultRouterTable_interface_ctx*
        if_ctx )
{
    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_ipDefaultRouterTable_container_init", "called\n" ) );

    /*
     * cache init
     */
    if_ctx->cache = CacheHandler_create( 30, /* timeout in seconds */
        _cache_load, _cache_free,
        ipDefaultRouterTable_oid,
        ipDefaultRouterTable_oid_size );

    if ( NULL == if_ctx->cache ) {
        Logger_log( LOGGER_PRIORITY_ERR,
            "error creating cache for ipDefaultRouterTable\n" );
        return;
    }

    if_ctx->cache->flags = CacheOperation_DONT_INVALIDATE_ON_SET;

    ipDefaultRouterTable_container_init( &if_ctx->container, if_ctx->cache );
    if ( NULL == if_ctx->container )
        if_ctx->container = Container_find( "ipDefaultRouterTable:tableContainer" );
    if ( NULL == if_ctx->container ) {
        Logger_log( LOGGER_PRIORITY_ERR, "error creating container in "
                                         "ipDefaultRouterTable_container_init\n" );
        return;
    }

    if ( NULL != if_ctx->cache )
        if_ctx->cache->magic = ( void* )if_ctx->container;
} /* _ipDefaultRouterTable_container_init */

/**
 * @internal
 * shutdown the container with functions or wrappers
 */
void _ipDefaultRouterTable_container_shutdown( ipDefaultRouterTable_interface_ctx* if_ctx )
{
    DEBUG_MSGTL( ( "internal:ipDefaultRouterTable:_ipDefaultRouterTable_container_shutdown", "called\n" ) );

    ipDefaultRouterTable_container_shutdown( if_ctx->container );

    _container_free( if_ctx->container );

} /* _ipDefaultRouterTable_container_shutdown */

ipDefaultRouterTable_rowreq_ctx*
ipDefaultRouterTable_row_find_by_mib_index( ipDefaultRouterTable_mib_index*
        mib_idx )
{
    ipDefaultRouterTable_rowreq_ctx* rowreq_ctx;
    oid oid_tmp[ asnMAX_OID_LEN ];
    Types_Index oid_idx;
    int rc;

    /*
     * set up storage for OID
     */
    oid_idx.oids = oid_tmp;
    oid_idx.len = sizeof( oid_tmp ) / sizeof( oid );

    /*
     * convert
     */
    rc = ipDefaultRouterTable_index_to_oid( &oid_idx, mib_idx );
    if ( MFD_SUCCESS != rc )
        return NULL;

    rowreq_ctx = ( ipDefaultRouterTable_rowreq_ctx* )
        CONTAINER_FIND( ipDefaultRouterTable_if_ctx.container, &oid_idx );

    return rowreq_ctx;
}
